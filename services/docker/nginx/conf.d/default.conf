server {
    listen 80;
    server_name localhost;
    default_type text/plain;

    location = / {
        return 200 "Local infrastructure is running.\n";
    }

    location = /health {
        access_log off;
        return 200 "ok\n";
    }
}
