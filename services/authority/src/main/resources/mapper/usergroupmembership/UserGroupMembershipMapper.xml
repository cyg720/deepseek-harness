<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.qs.authority.modules.usergroupmembership.UserGroupMembershipMapper">

  <sql id="columns">
    id, user_group_id, account_id, created_at, updated_at, created_by, updated_by, version
  </sql>

  <select id="findById" resultType="com.qs.authority.modules.usergroupmembership.UserGroupMembership">
    SELECT <include refid="columns"/> FROM qs__auth__user_group_account WHERE id = #{id}
  </select>

  <select id="countByPair" resultType="long">
    SELECT count(*) FROM qs__auth__user_group_account
    WHERE user_group_id = #{userGroupId} AND account_id = #{accountId}
    <if test="excludeId != null">AND id &lt;&gt; #{excludeId}</if>
  </select>

  <select id="list" resultType="com.qs.authority.modules.usergroupmembership.UserGroupMembership">
    SELECT <include refid="columns"/> FROM qs__auth__user_group_account
    <where>
      <if test="userGroupId != null">user_group_id = #{userGroupId}</if>
      <if test="accountId != null">AND account_id = #{accountId}</if>
    </where>
    ORDER BY ${orderBy}
    LIMIT #{limit} OFFSET #{offset}
  </select>

  <select id="count" resultType="long">
    SELECT count(*) FROM qs__auth__user_group_account
    <where>
      <if test="userGroupId != null">user_group_id = #{userGroupId}</if>
      <if test="accountId != null">AND account_id = #{accountId}</if>
    </where>
  </select>

  <insert id="insert" useGeneratedKeys="true" keyProperty="id">
    INSERT INTO qs__auth__user_group_account (user_group_id, account_id, created_by, updated_by)
    VALUES (#{userGroupId}, #{accountId}, #{createdBy,jdbcType=BIGINT}, #{updatedBy,jdbcType=BIGINT})
  </insert>

  <update id="update">
    UPDATE qs__auth__user_group_account
    SET user_group_id = #{userGroupId}, account_id = #{accountId}, updated_by = #{updatedBy,jdbcType=BIGINT}
    WHERE id = #{id} AND version = #{version}
  </update>

  <delete id="delete">
    DELETE FROM qs__auth__user_group_account WHERE id = #{id} AND version = #{version}
  </delete>
</mapper>
