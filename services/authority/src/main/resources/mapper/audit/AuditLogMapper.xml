<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.qs.authority.modules.audit.AuditLogMapper">

  <sql id="columns">
    id, trace_id::text AS trace_id, account_id, app_code, operation_code, resource_id, resource_type,
    request_ip, user_agent, result, reason, log_level, request_body, response_body, cost_ms, created_at
  </sql>

  <insert id="insert" useGeneratedKeys="true" keyProperty="id">
    INSERT INTO qs__auth__audit_log
      (trace_id, account_id, app_code, operation_code, resource_id, resource_type, request_ip, user_agent,
       result, reason, log_level, request_body, response_body, cost_ms)
    VALUES
      (CAST(#{traceId} AS uuid), #{accountId,jdbcType=BIGINT}, #{appCode,jdbcType=VARCHAR},
       #{operationCode,jdbcType=VARCHAR}, #{resourceId,jdbcType=VARCHAR}, #{resourceType,jdbcType=VARCHAR},
       #{requestIp,jdbcType=VARCHAR}, #{userAgent,jdbcType=VARCHAR}, #{result}, #{reason,jdbcType=VARCHAR},
       #{logLevel}, #{requestBody,jdbcType=VARCHAR}, #{responseBody,jdbcType=VARCHAR},
       #{costMs,jdbcType=INTEGER})
  </insert>

  <sql id="filters">
    <where>
      <if test="accountId != null">account_id = #{accountId}</if>
      <if test="appCode != null">AND app_code = #{appCode}</if>
      <if test="result != null">AND result = #{result}</if>
      <if test="createdAtFrom != null">AND created_at &gt;= #{createdAtFrom}</if>
      <if test="createdAtTo != null">AND created_at &lt;= #{createdAtTo}</if>
    </where>
  </sql>

  <select id="list" resultType="com.qs.authority.modules.audit.AuditLog">
    SELECT <include refid="columns"/> FROM qs__auth__audit_log
    <include refid="filters"/>
    ORDER BY ${orderBy}
    LIMIT #{limit} OFFSET #{offset}
  </select>

  <select id="count" resultType="long">
    SELECT count(*) FROM qs__auth__audit_log
    <include refid="filters"/>
  </select>

  <select id="findById" resultType="com.qs.authority.modules.audit.AuditLog">
    SELECT <include refid="columns"/> FROM qs__auth__audit_log
    WHERE id = #{id} AND created_at = #{createdAt}
  </select>

  <delete id="deleteOlderThan">
    DELETE FROM qs__auth__audit_log WHERE created_at &lt; #{cutoff}
  </delete>
</mapper>
