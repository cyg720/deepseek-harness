<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.qs.authority.modules.usergroup.UserGroupMapper">

  <sql id="columns">
    id, group_code, group_name, description, created_at, updated_at, created_by, updated_by, version
  </sql>

  <select id="findById" resultType="com.qs.authority.modules.usergroup.UserGroup">
    SELECT <include refid="columns"/> FROM qs__auth__user_group WHERE id = #{id}
  </select>

  <select id="countByGroupCode" resultType="long">
    SELECT count(*) FROM qs__auth__user_group
    WHERE group_code = #{groupCode}
    <if test="excludeId != null">AND id &lt;&gt; #{excludeId}</if>
  </select>

  <select id="list" resultType="com.qs.authority.modules.usergroup.UserGroup">
    SELECT <include refid="columns"/> FROM qs__auth__user_group
    <where>
      <if test="groupCode != null">group_code = #{groupCode}</if>
    </where>
    ORDER BY ${orderBy}
    LIMIT #{limit} OFFSET #{offset}
  </select>

  <select id="count" resultType="long">
    SELECT count(*) FROM qs__auth__user_group
    <where>
      <if test="groupCode != null">group_code = #{groupCode}</if>
    </where>
  </select>

  <insert id="insert" useGeneratedKeys="true" keyProperty="id">
    INSERT INTO qs__auth__user_group (group_code, group_name, description, created_by, updated_by)
    VALUES (#{groupCode}, #{groupName}, #{description,jdbcType=VARCHAR}, #{createdBy,jdbcType=BIGINT},
            #{updatedBy,jdbcType=BIGINT})
  </insert>

  <update id="update">
    UPDATE qs__auth__user_group
    <set>
      <if test="groupCode != null and groupCode.present">group_code = #{groupCode.value,jdbcType=VARCHAR},</if>
      <if test="groupName != null and groupName.present">group_name = #{groupName.value,jdbcType=VARCHAR},</if>
      <if test="description != null and description.present">
        description = #{description.value,jdbcType=VARCHAR},
      </if>
      updated_by = #{updatedBy,jdbcType=BIGINT},
    </set>
    WHERE id = #{id} AND version = #{version}
  </update>

  <delete id="delete">
    DELETE FROM qs__auth__user_group WHERE id = #{id} AND version = #{version}
  </delete>

  <select id="countMembers" resultType="long">
    SELECT count(*) FROM qs__auth__user_group_account WHERE user_group_id = #{id}
  </select>
</mapper>
