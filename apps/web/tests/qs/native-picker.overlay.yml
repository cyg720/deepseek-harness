# 固定原生 provider 由 launchWebScaffold 的 directoryPicker 选项负责。
- id: qs-shell
  config:
    defaultUi: workbench
    showOfficialUiEntry: false
- insert:
    - id: qs-native-picker-test
      name: '@deepseek-ai/dsh-qs-ui-directory-picker-native'
