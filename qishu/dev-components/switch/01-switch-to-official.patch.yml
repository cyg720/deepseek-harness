# 官方界面启动配置；仅用于包含 qs-shell 的 Web profile。
# 临时使用：pnpm dsh --profile web --patch ./qishu/dev-components/switch/01-switch-to-official.patch.yml
# QS 插件仍加载，共用官方业务服务；默认关闭开发者往返入口。
# patch 整值替换 config，必须使用当前的完整界面选择字段。
- id: qs-shell
  config:
    defaultUi: official
    showOfficialUiEntry: false
