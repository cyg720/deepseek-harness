# 第二优先 M1 接线登记

仅登记官方原文件；新建 qs 子目录文件、`packages/qs` 与 `qishu` 自有文件不在此登记。工作区与 HEAD `c5450ec399` 对比；本次不含上游同步。

本批为第二优先里程碑 M1（组件 C1–C3：工具调用树、过程与收尾、错误与重试/截断）所需的装配与生成目录增量。六个官方文件此前均已登记，本次按新目的再次修改；改动本身不新增官方业务源码行为，也不需要上游合并时改语义。

| 文件 | 原因 | 状态 | 验证与上游影响 | 差异 |
| --- | --- | --- | --- | --- |
| [tsconfig.base.json](../../tsconfig.base.json) | 为新二开包注册 `@deepseek-ai/dsh-qs-ui-tool` 与 `/client` 源码别名，使源码面解析（不依赖构建产物）成立 | 工作区已改，待提交 | 由 `pnpm run gen-tsconfig-paths` 生成并 `--check` 复检；上游合并保持“生成段由脚本维护、手写段只加 `/client` 子路径”的既有约定 | 见本批提交差异 |
| [tsconfig.client.json](../../tsconfig.client.json) | Client 聚合程序引用新包，纳入完整类型检查 | 工作区已改，待提交 | `node node_modules/typescript/bin/tsc -b tsconfig.client.json` 通过 | 见本批提交差异 |
| [packages/bundle/web-app/package.json](../../packages/bundle/web-app/package.json) | Web 组合声明新包为 workspace 依赖，Host 才能解析并分发其客户端插件 | 工作区已改，待提交 | 与 `cordis.patch.yml` 行同名同作用域；`pnpm install` 已更新锁文件 | 见本批提交差异 |
| [packages/bundle/web-app/cordis.patch.yml](../../packages/bundle/web-app/cordis.patch.yml) | 追加 `qs-ui-tool` 装配行，对应官方 `ui-tool` 行（官方行保留，供开发者切回） | 工作区已改，待提交 | 行 id/name 与包清单一致；配置门禁在本批 doc/CI 检查范围内 | 见本批提交差异 |
| [pnpm-lock.yaml](../../pnpm-lock.yaml) | 记录新工作区包的依赖解析结果 | 工作区已改，待提交 | `pnpm install --ignore-scripts` 实际执行结果 | 见本批提交差异 |
| [packages/extensions/cordis-client-runner/src/client/slot-catalog.ts](../../packages/extensions/cordis-client-runner/src/client/slot-catalog.ts) | 从源码重新生成客户端槽位目录：新增 `qs.tool.call.toolview` 与 `tool-call` 行键的公开记录 | 工作区已改，待提交 | `tsx scripts/gen-client-catalog.ts` 重新生成、`--check` 复检通过 | 见本批提交差异 |

说明：

- `packages/qs/qs-transcript/**` 与 `packages/qs/qs-ui-tool/**` 是二开自有文件，不在本清单登记；本次对转写包的行 owner 契约扩展（图片装载、过程折叠状态）属于二开自有接口，不涉及官方原文件。
- 本批不新增官方业务源码改动：没有修改 `packages/client/**`、`packages/api/**` 或任何 Host 侧实现。
- M1 的实现与验收证据见[第二优先 M1 验收报告](../PRD/1-AI工作台/复核测试/32-第二优先M1工具过程诊断验收.md)。
