# 第二优先审查修复与全量验收

状态：进行中，尚未完成整体验收。范围依据 [审查结果 v1](../../../dev-components/第二优先开发计划评审/审查结果-v1.md)，覆盖 D1–D7 与 W1–W12；用户明确本期延期 W11，保留说明入口，不接真实登录。其余需求没有缩减。

## 已修复与证据

2026-09-22：R05 待办执行失败不再用请求清单覆盖失败结果，回落通用卡保留错误正文；R07 授权图片 URL 返回后浏览器加载或解码失败显示可访问的错误提示，保留结果正文。只改动 QS 自有代码、测试和文档，不涉及官方原文件。

执行 `node node_modules/vitest/vitest.mjs run packages/qs/qs-ui-tool/tests/toolviews.client.spec.tsx`：修复前 26 条中 2 条失败，分别复现错误正文丢失、图片错误提示缺失；修复后 26 条全部通过。这是定向 DOM 回归，不能替代真实浏览器、完整构建或全量验收。

2026-09-22 修复 R06：对照官方 ask-question-row.tsx 的 answeredPresentation，有效但未配对的回答按实际结果条目统计；无效结果回落通用卡保留原文。部分回答、自定义回答、重复 ID、未知 ID、非法结果和 DOM 数量文案回归在修复前两条失败，修复后工具包 7 文件、59 条通过。

2026-09-22 修复 R02：统一折叠与展开的活动判定，纳入外部过程和内联推理；未加载完整历史或轮次不匹配时不隐藏成员。转写区真实过程按钮的 DOM 测试覆盖零计数展开/收起、答案不重复和历史缺页保护，修复前两条失败，修复后转写包 8 文件、84 条通过；包级 TypeScript 构建通过。独立审查探针 7 条中 6 条通过，仍有 R03 凭据摘要失败；其中性能探针只是量化现有平方级扫描，不代表性能通过。

2026-09-22 修复 R03/R09：折叠摘要不自动展示命令原文，明显凭据头、赋值及带认证/查询信息 URL 整项省略；DOM 回归证明只有主动展开后才出现原始命令，收起后移除，载荷不变。规则不能识别所有自由文本秘密。时间单位移入字典；协议字段键改用准确的 ARGUMENT_KEYS 命名，未修改或放宽国际化检查器。检查覆盖 697 个 Client 源文件通过；两个包 15 文件、145 条测试通过。

R03/R09 修复后补充验证：审查探针 7 条全部通过（性能项仍为现状计数量化）；两个包的 TypeScript 构建、Oxlint 和两组变更双语配对检查通过。静态检查发现的五项本批问题均已修复，最后重跑两个包 145 条仍全部通过。

2026-09-22 修复 R04 重复扫描：对照官方 MutableChatNodeStore 的 values/replace/upsert，按节点数组引用建立弱缓存轮次诊断索引；每份快照扫描一次。1000 节点、100 个收尾查询两类诊断的带失败节点回归，修复前读取 kind 199001 次，修复后 1000 次；新增、删除、新旧快照并存的事实回归通过。转写包 8 文件、85 条通过。这证明该算法的重复工作减少，不证明端到端浏览器耗时、堆占用或长会话 SLO 已通过，完整性能验收仍待执行。

2026-09-22 修复 R08：运行中从官方节点位置的同轮 steps 取最新步骤，结束后取 answerStep；不靠计数推测。DOM 验证步骤 1/2/3，在修复前失败、修复后通过，转写包 8 文件、86 条通过；模型补充不同轮、缺位置、终态答案步骤优先回归。真实服务流式推进的集成验收仍待执行。

2026-09-22 R11 部分修复：通用参数和非文本结果改用官方 DisclosureRow，序列化组件仅在用户打开对应详情后挂载。合成约 1 MB 参数/结果的回归在修复前失败，检查关闭时零解析/零序列化、展开后完整内容、收起移除 DOM、原文转义。错误正文保持直接可见。文本拼接、宽树及真实浏览器展开预算尚待处理，R11 不标为全部完成。

R11 懒计算调整后：工具包 7 文件、62 条测试、包级类型、Oxlint、双语配对通过。随后完整 build 发现测试槽位泛型断言错误（包级构建不包含该测试）；已修正并正在重新构建。R13 已补真实过程展开/收起/键盘回归及明确缺指标的收尾快照，待新产物浏览器运行证据。

新产物验证：重新执行完整 pnpm run build 通过；QS 浏览器 5 文件、11 条中 9 通过、2 失败。transcript-follow 的发送跟随与新收尾快照通过；disclosures 在 760 宽度的新增再次收起点击被左导航拦截；interaction-roundtrip 历史前插锚点偏移约 155px，需核查折叠与滚动联动。没有放宽定位断言或强制穿透点击，R13 仍未关闭。日志：logs/repair-v1-build.log 与 logs/repair-v1-browser.log。

2026-09-22 关闭 R13 已复现浏览器失败：诊断指针按下前/时锚点均为 638.078125，分页后变为 482.890625，确认并非点击预滚动。修复分页锚点跨后续布局保持、用户滚动/发送解除、原生锚定暂时禁用并在卸载恢复；新增二次 resize/解除/恢复的 DOM 回归通过。窄屏通过真实导航按钮收起覆盖层后继续正文交互，无强制点击。完整 build 再次通过，新产物 QS 浏览器 5 文件、11 条全部通过（48.85 秒）；不据此宣称尚未实现的 D/W 功能通过。

本轮 QS 覆盖率复核：57 文件、357 条测试通过；首次覆盖率检查发现两个视图模型缺分支，补充同轮失败与截断共存、未知轮次不污染、空描述回退断言后重跑退出 0，QS 源码逐文件四项 100%。这只覆盖当前 QS 源码，不包含尚未创建的插件，也不等于全仓库验收。日志：logs/repair-v1-coverage.log。

## D4 自动历史分页阶段验证（2026-09-22）

对照官方 `packages/api/session-controller/src/client/sessions/session.ts` 的 `loadOlder` 与 `historyLoad` 实现，QS 保持官方请求串行、分页失败和会话有效性判断的唯一职责，仅增加真实滚动容器顶部观察器和锚点捕获。断线、加载中、失败、取消及无进展时暂停自动触发；旧会话和卸载后的回调不能请求新页，手动重试保留。

转写包 9 文件、90 条测试通过；客户端 TypeScript build 检查、转写包 lint 通过。完整 `pnpm run build` 通过。新构建下 `interaction-roundtrip.e2e.ts` 5 条真实浏览器测试通过，其中分页用例通过滚动触发自动加载，不点击加载按钮，并验证原行坐标保持。转写包定向覆盖率四项 100%（语句 453/453、分支 358/358、函数 136/136、行 348/348）。README 与 Agent Note 两组配对检查通过。

证据：`logs/repair-v1-history-browser.log`、`logs/repair-v1-history-coverage.log`。浏览器失败暂停与断线恢复的真实网络时序仍需补充集成证据；目前由可控观察器 DOM 测试覆盖。以上结果不代表全仓库测试、完整 D4 验收或第二优先整体通过；其他待完成事项继续有效。

## D6 压缩行阶段验证（2026-09-22）

已在 qs-transcript 注册 compaction 和 manual-compaction 专属行。依据官方 compactionDefinition、CompactionSummaryNode 和 ManualCompactionChatData，自动压缩只展示已落地标记，手动压缩消费官方命令/checkpoint 关联结果，不重新关联事件或执行命令。错误与无 checkpoint 结算保留全文，缺摘要和缺估算明示，摘要按展开状态挂载，真实零值与未知值区分。

先执行新增回归，确认旧实现两条失败：压缩落入未知 JSON 行，且没有执行中状态。实现后转写包 9 文件、94 条测试通过；定向逐文件覆盖率检查通过，四项 100%（语句 468/468、分支 382/382、函数 141/141、行 360/360），HTML 报告包含新增 compaction-rows.tsx。客户端类型检查、定向 lint、699 个客户端源码的 i18n 检查和两组文档配对检查通过。

完整 build 通过。真实 Host＋浏览器 transcript-media.e2e.ts 两条通过，新增场景沿用官方 seeded-history 压缩事务结构并使用 Host tokenMeter 对原消息估值，验证手动记录唯一、原始消息保留、模型 checkpoint 信封不显示和摘要展开；新增可读输出快照已人工核查。测试脚本位于既有 QS Host 测试文件，不需要修改官方编译配置。

证据：logs/repair-v1-compaction-red.log、logs/repair-v1-compaction-coverage.log、logs/repair-v1-compaction-build.log、logs/repair-v1-compaction-browser.log。当前仅完成压缩行及上述场景；上下文压力面板尚未落地，自动压缩、缺窗、失败和取消的完整浏览器矩阵仍需补齐，因此 C6/D6 不标记整体验收完成。本批次全部修改位于二开自有文件，不新增官方源码修改登记。

## D6 上下文压力面板阶段验证（2026-09-22）

qs-composer 新增 ContextPressure，直接使用官方会话 useProjection 的 contextPressure/contextBreakdown，不创建计量服务。优先 projectedTokens，再取 pressureTokens；容量或采样缺失时显示“暂无上下文估算”，零采样保持真实零值。显示占比上限为 100%，展开仍保留原始估算数量。面板说明模型切换采样时间差、启发式组成不一定相加等于占用，以及这些数值不是计费或本轮实际用量。容量撤销后关闭展开状态。

输入包 47 条测试通过。定向覆盖率初跑发现 Host 空入口未由输入包测试独立调用，加入已有 qs-shell Host 装卸测试后，共 9 文件 51 条通过，输入包逐文件四项 100%（301/301 语句、189/189 分支、106/106 函数、230/230 行），未新增无意义的重复 Host 测试。客户端类型检查、定向 lint、700 文件客户端 i18n 检查、两组双语配对检查与完整 build 通过。

真实 Host＋浏览器 transcript-follow.e2e.ts 通过，已结算请求投影显示 7941 / 128000 tokens、6%，系统/工具/消息估算为 1766/7127/536。新快照人工核查，明确组成合计与占用不同；场景同时保留主动发送后的滚动跟随断言。证据：logs/repair-v1-pressure-coverage.log、logs/repair-v1-pressure-build.log、logs/repair-v1-pressure-browser.log。

唯一官方原文件增量是 pnpm-lock.yaml 的 qs-composer→token-meter 工作区类型依赖，已登记到官方总表及 15-上下文压力依赖登记.md。使用离线 lockfile-only 安装，未升级第三方版本。D6 的完整异常浏览器矩阵、截图及整体第二优先验收仍待继续，C6 任务卡保留未验收状态。

## D7 命令结果行阶段验证（2026-09-22）

qs-transcript 新增 command 行，直接读取官方 CommandNode。运行、成功和失败状态可辨，name 缺窗时使用通用标题；不自动回显参数，不提供重执行按钮。完整结算文本按需挂载为普通文本，展开与关闭不丢失原始结果；脚本文本不执行。

新增行为测试先证明旧实现缺少命令运行状态，再实现。转写包 9 文件、96 条测试通过；定向逐文件覆盖率四项 100%（477/477 语句、394/394 分支、144/144 函数、367/367 行）。定向 lint、双语配对检查与完整 build 通过。真实 Host＋浏览器 transcript-media.e2e.ts 两条通过：新增独立 command/run+command/done 失败事务，经真实投影显示一条命令行，与手动压缩区分；完整多行错误可展开，参数不回显，输出快照已生成并核查。

证据：logs/repair-v1-command-red.log、logs/repair-v1-command-coverage.log、logs/repair-v1-command-build.log、logs/repair-v1-command-browser.log。此场景验证持久日志到界面的呈现，不证明命令入口可执行。qs-ui-input-trigger、qs-ui-commands 的独立呈现插件、键盘与 IME 仲裁、Host claim、confirmation、并存冻结原因及跨会话焦点恢复仍待落实。command-input 依计划归 W7/C15；不得据本次结果把 D7 标为整体完成。本批没有修改官方原文件。

## D7 命令输入与冻结前置验证（2026-09-22）

发现 QS 把 command claim 与引用/附件一起禁用，阻断已认领命令补参数。对照官方输入 facade 的 setDraft、claim 观察和 submit 流程后，允许纯文本命令 claim 编辑及提交，仍由官方输入机保存 claim 身份和执行，不直接调用 claim.submit；结构化引用与附件继续保护。新增用例先失败再修复，覆盖补参数、IME Enter 不提交和普通 Enter 交给输入机。

为后续独立弹层接入增加 acquireFreeze：命令与确认各自持有原因，创建交接保留独立 sending 状态；释放幂等且只解除自身原因。卸载清除持有者，旧释放/取得/发送冻结回调不影响状态。命令冻结不显示创建交接专用的取消按钮或“发送中”标签。当前 API 已通过插件生命周期测试，尚未接入仍未创建的 QS 命令弹层，不能当作整个弹层完成。

真实浏览器首次发现展开压力面板后输入区挤掉转写视口，命令虽然执行成功但结果按钮被 composerZone 拦截。修改输入区布局：最大占主区一半，过长内容内部滚动；未强制点击或关闭面板规避失败。新构建重跑通过：QS 输入 /permission read-only，经真实官方 Host 执行并得到 preset read-only 结果，草稿清空，转写区高度大于零，压力面板展开时结果按钮可正常点击。此场景证明带参命令入口，不证明尚未实现的菜单选择与 confirmation。

检查结果：输入包及既有 Host 装卸测试共 9 文件、53 条通过，逐文件覆盖率四项 100%（317/317 语句、196/196 分支、110/110 函数、241/241 行）；完整 build、定向 lint、701 文件 i18n 检查、双语配对检查和定向 diff 空白检查通过。证据：logs/repair-v1-claim-red.log、logs/repair-v1-freeze-red.log、logs/repair-v1-claim-coverage.log、logs/repair-v1-claim-build.log、logs/repair-v1-claim-browser.log。所有本批修改均为 QS 自有文件，无新增官方原文件登记。

仍需落实：qs-ui-input-trigger 与 qs-ui-commands 独立插件、真实选区/draftRev 同步、菜单按键仲裁、客户端 action、确认弹层、焦点恢复及切换会话迟到结果隔离。D7 和第二优先整体继续保持未验收。

## D7 独立候选插件阶段验证（2026-09-22）

新增 packages/qs/qs-ui-input-trigger，对应官方 ui-input-trigger 的候选呈现；复用唯一官方 inputTriggers 控制器，输入区继续拥有消费者租约。本插件贡献 qs.composer.overlay，注入官方会话输入和控制器，显示加载及候选，鼠标/键盘选择由官方服务处理。输入桥以真实 draftRev、当前光标和 phase 同步，IME 期间冻结候选；菜单消费 Enter 后不落到普通发送。旧输入绑定释放仅移除自身，卸载关闭菜单并清理订阅/外部点击监听。

验证：全部 QS 63 文件、375 条测试通过；逐文件四项覆盖率 100%（2357/2357 语句、1706/1706 分支、712/712 函数、1845/1845 行）。新增组件/插件生命周期覆盖重装、缺会话、候选高亮、鼠标选择、旧绑定迟到卸载和 Host 无 DOM 装载。客户端类型检查、定向 lint、705 文件客户端 i18n 检查、完整 build、源码别名及客户端目录生成复检通过。

真实 Host＋浏览器 transcript-follow 场景通过：/goa 出现 goal 候选，键盘选择后草稿进入可编辑 /goal，菜单关闭；接着 /permission read-only 仍执行并产生真实成功结果。证据：logs/repair-v1-trigger-build.log、logs/repair-v1-trigger-browser.log、logs/repair-v1-trigger-coverage.log。原有压力估算和主动发送滚动跟随断言同时保留。

官方原文件六处配置/生成文件已记录到 16-命令候选插件接线登记.md 及当前工作区差异快照；二开源码未纳入官方清单。qs-ui-commands 的选项弹层与确认仍未实现，候选目录失败/空匹配的可见恢复和完整切换会话时序仍需补齐。此结果不是 D7 或整个第二优先验收完成。

## 待完成

定向扩大回归：`node node_modules/vitest/vitest.mjs run packages/qs/qs-ui-tool`，7 个测试文件、59 条全部通过；README 与已有 Agent Note 两组双语配对检查通过；本批次定向 `git diff --check` 通过。没有重新执行完整仓库测试或真实浏览器验收。

R01、R10–R12；R04 端到端性能验收 仍需处理或补足证据；R02/R03/R05/R06/R07/R08/R09 还需纳入最终集成验收。D4–D7 与 W1–W10、W12 的功能落地、插件逐项对应、异常与卸载回归、性能安全检查和全量测试仍待完成。W11 的延期说明入口也需验证，不能把延期当作已实现第三方集成。

## 命令选项与风险确认（2026-09-22）

对照官方 `ui-commands` 的 `popup.ts`、`PopupSelectView.tsx`、`service.ts` 以及权限预设装饰实现，增加独立 `packages/qs/qs-ui-commands`。官方 commandUi 服务保持唯一，QS 只拥有选项呈现、输入冻结和焦点订阅；不重新实现命令执行和草稿消费。关闭或卸载撤销迟到结果的界面写入和消费权，但不能撤销已经发出的业务操作。

- 覆盖：加载失败重试、空搜索结果、标签/描述忽略大小写匹配、键盘选择、中文输入法隔离、风险确认与取消、单次执行、提交期只读、成功消费与焦点恢复、外部点击关闭、卸载中止加载和迟到执行、列表局部滚动、插件卸载重装。
- 修正两个新增纯客户端插件的 Host 空入口测试后缀为 `host.client.spec.ts`，按现有 qs-shell 约定纳入客户端编译面，仍在无 DOM 的 Node 环境运行。修正跨功能插件运行时导入，官方控制器类型仅作类型导入；本地筛选与官方语义一致，不增加 external 例外。
- QS 覆盖率命令通过：66 文件、381 条测试；语句 2439/2439、分支 1778/1778、函数 743/743、行 1902/1902，逐文件四项均为 100%。日志 `logs/repair-v1-popup-coverage.log`。
- 完整 `pnpm run build` 通过，日志 `logs/repair-v1-popup-build.log`；最终键盘修正后再次客户端类型检查、独立插件 bundle、定向 lint 均通过。
- 真实 Web/Loader 场景通过：裸 `/permission` 打开弹层，未勾选风险不可提交、取消保留草稿、选 Read Only 后消费命令并恢复焦点。日志 `logs/repair-v1-popup-browser.log`。最终全 QS 浏览器复跑 5 文件、12 条场景通过（57.94 秒），日志 `logs/repair-v1-popup-browser-all.log`。
- 708 个客户端 UI 文件本地化检查、3 对文档翻译配对、源码别名与槽目录生成检查通过。

官方原文件变动另见 [17 登记](../../../官方源码改动记录清单/17-命令选项插件接线登记.md)。以上不等于 D7 或第二优先整体完成：命令目录失败与空态、跨会话完整场景、其余 W 组功能、全仓验收和可达依赖安全复查仍有待办。W11 按用户决定延期并保留说明入口，不接真实登录。

## 消息评价与会话反馈（2026-09-22）

按官方 `ui-message-feedback` 独立职责新增 `packages/qs/qs-ui-message-feedback`，分别注册消息动作与会话表单。复用 M0 公开的 `messageFeedbackPresentation`，不实例化第二份 FeedbackSurface，也不重复注册 `/feedback`。转写收尾行只向 `qs.chat.assistant-actions` 传入真实持久 messageId，中断残片和运行中消息不伪造评价对象。

- 消息评价：延迟加载、赞/踩、撤回、同批点击去重、读取失败重试、版本冲突提示、切换对象与卸载后的迟到结果隔离。
- 会话表单：固定显示会话及消息对象、官方七分类、可取消分类、完整文本、提交中禁改/禁重复、失败保留、取消无写入、成功确认、切换后保留官方草稿、关闭不声称撤销已发操作。原生 dialog 留在 QS 根内继承主题，由浏览器约束并恢复焦点。
- 真实载荷：DOM 用例直接用官方 FeedbackDialogController，断言精确 messageId/rating/category/trimmed text。真实 Web/Host 验证 8193 字符拒绝后保留草稿、成功持久化、冷刷新重读及撤回、裸 `/feedback` 会话目标、带参 `/feedback` Host 事件；不触发模型轮次。
- QS 70 文件、395 条测试通过；逐文件覆盖率语句 2529/2529、分支 1835/1835、函数 772/772、行 1962/1962，四项均 100%。日志 `logs/repair-v1-feedback-coverage.log`。
- 官方反馈相关回归 10 文件、145 条通过，日志 `logs/repair-v1-feedback-official.log`。
- 完整构建通过，日志 `logs/repair-v1-feedback-build.log`；原生 dialog 调整后客户端类型检查、独立插件 bundle、定向 lint 均通过。712 个客户端文件本地化检查、4 对文档配对、两项生成目录检查通过。
- 首轮全 QS 浏览器有 1 条测试失败：冷刷新后静态登录按设计失效，测试遗漏重新登录。补回实际登录流程后，定向 2 条场景通过并记录新反馈表单快照，日志 `logs/repair-v1-feedback-browser-final.log`。全 QS 浏览器最终复跑 5 文件、13 条场景通过（62.53 秒），日志 `logs/repair-v1-feedback-browser-all-final.log`。中间复跑还发现从未打开的表单卸载时调用 close，已限制为实际打开的模态才清理；没有关闭测试异常检查或模拟成成功。

官方配置修改见 [18 登记](../../../官方源码改动记录清单/18-消息反馈插件接线登记.md)。W2 仍未整体完成：G07 的任务完成通知、去重、重连基线与 TTL/maxEntries 尚未落地；反馈确认提示不能抵扣该项。第二优先整体及全仓验收仍未完成，W11 维持用户授权延期。

### 本轮文档门禁复核

`pnpm run test:docs` 最终 16 项全部通过（16.31 秒），日志 `logs/repair-v1-feedback-docs.log`。检查发现的 README metadata、Model Experience 字段/锚点位置、限制列表、开发备注及 LF 换行均已修正，涵盖最近新增的命令候选、命令选项和反馈三个包；对应中英文及配对记录同步更新。未降低门槛或加入例外。此命令不是完整 doc-sync，也不能代替全仓测试。

## 命令风险确认主题与模态回归（2026-09-22）

对照官方 RiskConfirmation 与 Modal 实现，确认原先 body portal 不属于 QS 主题根。qs-ui-commands 现使用 QS 根内的原生 dialog；只替换呈现，确认状态、未勾选禁止执行、成功消费和迟到请求处理仍由唯一官方 PopupSelectController 负责。显式关闭或 Escape 返回命令选项，不执行风险操作；卸载关闭原生模态并释放输入冻结。没有修改官方原文件。

- DOM 回归：3 文件、7 条通过，包含确认文字、未勾选禁用、取消及关闭按钮、Escape、卸载关闭、精确官方 onSelect 载荷和草稿消费断言。jsdom 原型替身在每个用例后恢复；浏览器事实没有靠 jsdom 替身证明。
- 定向覆盖率四项 100%：语句 94/94、分支 72/72、函数 36/36、行 66/66，见 `logs/repair-v1-command-modal-coverage.log`。最终仅调整引用断言及测试替身持有方式后，再跑 7 条测试通过。
- 真实 Web/Host：2 条场景通过，断言 :modal、QS 根归属、已解析主题变量、焦点位于对话框、Escape 保留 /permission 草稿、风险取消后可继续选择。新增风险确认文案快照，检查后不带更新参数复跑通过，见 `logs/repair-v1-command-modal-browser-final.log`。
- 客户端类型检查、独立插件 bundle、定向 lint、712 文件 UI 本地化检查、2 组配对检查及定向 git diff --check 通过。`pnpm run test:docs` 16 项通过，见 `logs/repair-v1-command-modal-docs.log`。没有重跑完整仓库测试或完整 build，本批不声称全量验收完成。

右栏继续待接入。官方源码已核实：sidebarRightPresentation 提供唯一 store 句柄、seat 绑定与 tabInfo 注入；TabDomain 随 session store 提交管理标签生命周期，不能复制 store 工厂。QS 当前 RightPanel 仍为静态空态，且 qs-shell.rightOpen 与官方 layout.expanded 分别持有展开状态，正式接入必须明确同步方向并验证公开 openTab 在折叠状态下能显示内容。以上只是源码核对结论，不标记 W4 已实现。

## C12 公共右栏 session 宿主与官方共享状态（2026-09-22）

qs-ui-sidebar-right 取代静态三标签，复用官方 sidebarRightPresentation 的相同 store 句柄、seat 绑定、tabInfo hook 和唯一 TabDomain。没有复制 store 工厂或资源 pin 服务。正文、标题、菜单和 guide chain 各自保持官方对应职责；缺失正文显示限制，向导只允许打开已装配正文的类型。官方公共 docking 组件处理手势和布局意图；QS 提供局部主题与文案，浮窗留在 QS 根内。

外壳新增仅内存的 rightRequestId 区分用户/视口开合请求与面板实际状态回报；reportRightOpen 不增加请求序号。首次会话按外壳偏好初始化，已有会话按共享官方状态呈现；官方导航可重新显示折叠面板。删除无业务语义的静态 activeRightTab 持久化，真实会话标签由官方唯一 store 管理。刷新后的真实标签恢复仍是未完成需求，并未用旧静态标签索引替代。

- slot runtime 使用真实官方插件、store 和控制器：验证关末标签/重播种、折叠后打开、会话 A/B 导航、资源 pin 与 abort、QS 卸载重装保持官方 store、缺失正文与后装配、guide chain 和菜单、分栏/浮窗/尺寸意图。
- QS 覆盖率首次扩大复跑 71 文件、402 条测试通过，逐文件四项 100%：语句 2639/2639、分支 1871/1871、函数 827/827、行 2041/2041。日志 `logs/repair-v1-right-panel-coverage-all.log`。
- 真实 Web 专项初跑通过：从 QS 拖出并回收浮窗，QS→官方→QS 保持真实 tab ID，折叠再打开，390px 下无整页横向溢出，注册入口文案写入 keyless snapshot。日志 `logs/repair-v1-right-panel-browser.log`。
- 扩大浏览器首轮 13 条通过、1 条失败：旧场景仍按第三个静态占位标签定位。该场景改为验证真实外壳显隐/尺寸恢复；真实标签跨界面身份由新增专项检验。真实标签刷新恢复尚未补齐，不隐藏这一缺口。
- 新增独立 e2e 文件曾误入 apps/web 的客户端编译面；已合并到现有 Host 场景文件。只删除经过时间、仓库路径和对应 TS 源文件核实的 500 个本轮生成未跟踪产物，未删除先存文件；确认没有跟踪中的 .js/.d.ts/.map 原文件被改动。没有改官方 Web 编译配置。
- 修正之前 useHistoryScroll 的参数文档与导出签名不匹配问题，保持函数行为不变。客户端编译通过；导出 JSDoc、本地化（716 文件）、定向 lint 及槽目录生成一致性检查通过。最终文档/浏览器复跑结果补在本节后。

官方原文件只有锁文件和自动生成槽目录的追加修改，登记于 [19 批次](../../../官方源码改动记录清单/19-右栏共享座位接线登记.md)。C12/W4 尚未完整完成：刷新后标签布局恢复、版本迁移、全套键盘与缩放验证、公共 docking 菜单 portal 的 QS 主题、Chat/trajectory 视图选择仍待处理；W1 等业务正文继续按独立插件接入。没有接入真实登录，W11 维持延期。

### 本批追加核查

最终 QS 覆盖率复跑 71 文件、402 条全部通过，四项逐文件 100%：语句 2640/2640、分支 1871/1871、函数 827/827、行 2042/2042，见 `logs/repair-v1-right-panel-coverage-all-final.log`。完整 `pnpm run build` 通过，见 `logs/repair-v1-right-panel-full-build.log`。官方右栏回归 9 文件、138 条通过，见 `logs/repair-v1-right-panel-official.log`。`test:docs` 16 项通过，见 `logs/repair-v1-right-panel-docs.log`；源码别名、导出 JSDoc 和最终定向 lint 检查也通过。

旧浏览器场景调整后又发现测试试图直接点击折叠面板里的收起按钮。几何采样确认面板宽度 0、inert=true，与官方→QS 保留折叠状态相符，并非可见面板被覆盖。已先通过顶栏展开再测试收起，不使用 force 点击；定向问题场景通过，见 `logs/repair-v1-right-panel-question-final.log`。文档明确初始显隐偏好与已有官方会话状态的区别，不能声称所有布局状态已经支持刷新恢复。

最终全部 QS 浏览器回归 5 文件、14 条场景通过（62.88 秒），无快照更新参数、无跳过场景，见 `logs/repair-v1-right-panel-browser-accepted.log`。官方原文件登记状态同步更新，FRAMEWORK 删除过时的“十包”和“命令选项未实现”说法，当前十四个 QS 插件按官方职责列出。完整仓库单元/覆盖率、性能安全总验收及其余第二优先功能仍未完成；本批通过不代替整体目标完成。

## 全屏面板顶栏遮挡修复（2026-09-22）

源码复核确认 QS 全屏面板固定顶部偏移 54px，与顶栏在各断点下的 61/68/73px 不一致。新增真实浏览器几何断言先在旧实现失败，见 `logs/repair-v1-panel-geometry-before.log`，证明既有仅检查宽度的测试漏掉了纵向遮挡。面板改为在外壳定位内容区内绝对铺满，不复制顶栏高度或新增监听器；随断点和视口高度由浏览器布局计算。

专项验证 1400→1700→390→1400px 下，面板上沿等于实际顶栏下沿，左右及底边贴合视口；同一场景继续验证官方↔QS 标签身份、浮窗回收、折叠展开与窄屏无横向溢出。专项通过后，完整 transcript-follow 浏览器文件 3 条全部通过（16.68 秒），见 `logs/repair-v1-panel-geometry-regression.log`。右栏 bundle、定向 lint、README 中英文配对检查通过。新增测试的箭头括号 lint 问题已修正，不改变行为。本批未改官方原文件，不新增官方源码登记。

本次只修复已经复现的宿主几何缺陷；文件树、六类文档预览、布局刷新恢复及其余第二优先待办未因本次通过而关闭，完整仓库与安全性能验收仍待完成。

## 依赖安全修补进度（2026-09-22）

重新在线审计发现 25 high、28 moderate、3 low；按上游已修复同主版本定向更新 fast-uri 3.1.6、brace-expansion 2.1.4/5.0.9 后，审计降低为 14 high、28 moderate、3 low，两种依赖不再命中公告。MCP/E2B 127 条单元回归、MCP 本地真实协议 22 条、完整 build 均通过。官方变更仅 workspace override 与锁文件，已登记 20 批次；详细可达性判断、补丁依据、剩余 14 条高风险和日志见 [34 安全复核](34-依赖安全补丁与可达性复核.md)。没有关闭安全门禁，也没有将构建成功等同功能或性能全量验收。

第二批定向安全升级完成：undici、ip-address、nanoid、PostCSS、browserslist 使用现有主版本已修复版本，高风险从 14 降至 7，中风险从 28 降至 20。QS/HTTP 代理/MCP/E2B 共 618 条、完整 build、全部 QS 浏览器 14 条通过。浏览器扩大回归曾暴露跨断点测试时序，等待外壳状态提交后复跑通过，保留失败日志；不是更新快照或强制点击绕过。extract-zip 公告修复版 2.0.2 在 npm 官方源不存在，未留下不可安装 override，风险保持未解决。详细依据、剩余事项与官方登记见 34 报告和 20 登记；本批不表示整体完成。

pnpm 开发/桌面 CLI 已统一升级 11.11.0，冻结安装、195 条桌面测试与桌面 build 通过；1 条 macOS 专属签名测试在 Windows 跳过。安全 audit high 从 7 降至 3，剩余 Vite 与 extract-zip。总表新增根 package.json 与 apps/desktop/package.json 并按路径重排，共 114 项；详见 34 安全报告与 20 登记。整体功能、安全及性能验收仍未完成。

文档站 Vite 已定向升至 6.4.3，冻结安装、完整文档站构建、2668 内部锚点验证及临时开发服务器敏感路径拒绝烟测通过。审计 high 降至 2，均为 extract-zip 未解决项；moderate 17、low 3。详见 34 报告，整体验收仍未完成。

## 文件树共享呈现接口（2026-09-22）

核对官方 ui-sidebar-files 的 store、face 和 ui-renderer 的按句柄/session 缓存规则后，公开 sidebarFilesPresentation。官方注册本身已使用该共享对象；同一 store actions 对应唯一 face，避免官方/QS 两个注入工厂各自持有请求代次，导致切换界面后旧响应覆盖新刷新。会话实例隔离，标签 abort 继续清桶和拒绝迟到写入。新代码放在官方目录的 qs 子目录，官方原文件变更登记 21 批次。

官方文件树 7 文件 38 条测试通过，逐文件四项 100%（语句 158/158、分支 63/63、函数 58/58、行 135/135），包括两消费者交错刷新、关闭标签迟到返回、不同会话隔离以及官方注册/卸载使用同一服务。首轮类型检查发现新测试伪造 RemoteFailure 不符合真实类型，已改用 RemoteError 后 Client 类型检查通过；lint 的箭头括号也已修正。test:docs 16 项通过。日志 repair-v1-files-shared-tests-final.log、repair-v1-files-shared-typecheck-final.log、repair-v1-files-shared-docs.log。

这只是 C11 所需共享数据接入，QS 文件树正文、真实浏览器双界面状态交接、文件预览和工作区选择仍未完成；不把新增接口等同业务交付。没有新增 Host 读取接口、权限放宽或真实登录。

## 文档预览与当前 QS 回归（2026-09-23）

六类预览已接入，官方与 QS 共享内容和页码状态，PDF 复用原 worker。当前全部 QS 包覆盖率为 81 文件、451 测试通过，四项逐文件 100%；定向真实 Web 组合场景通过。详情见 [41-PDF](41-PDF预览阶段验收.md) 和 [42-分支回归](42-文档预览分支与取消回归.md)。C9 的附件/可交付物仍未完成，其余未交付 D/W 功能及全仓、安全性能总验收保持待办，本结果不代表完整第二优先完成。

## 可交付物独立呈现（2026-09-23）

新增 qs-ui-deliverables，对应官方轮次收尾和 present 工具贡献；复用原事件投影、共享请求控制器和文件资源路由。当前全部 QS 83 文件、460 测试通过，逐文件四项 100%；完整 QS 浏览器 5 文件、15 场景通过。见 [43 报告](43-可交付物接线阶段验收.md)。行内文件引用与附件四入口仍待实现，C9 不标完整完成；其余未交付 D/W 功能继续保留。

## 最终回答文件引用（2026-09-23）

已复用官方 chatFileMentions 并按官方最终回答条件启用，全部 QS 84 文件、463 测试通过，逐文件四项 100%；定向真实浏览器验证点击预览。见 [44 报告](44-最终回答文件引用阶段验收.md)。附件四入口仍待实现，未关闭 C9 和整体验收。


本轮完整 doc-sync 为 30 通过、4 失败：服务文档映射、公开状态显式类型、配置目录过期与源码路径目录不同步；具体失败及日志见 44 报告。QS 浏览器 5 文件 15 场景均通过，仍不能声明整体验收通过。

## 目录门禁修复（2026-09-23）

44 报告的四项失败已处理，完整 doc-sync 34 项通过；Client 编译和定向 lint 通过。见 [45 报告](45-共享呈现目录门禁修复.md)。其余业务与整体验收待办保持不变。

## 附件范围校正（2026-09-23）

核对原始第二优先第 46、78 行：本期只包含已有附件查看，新上传排除。为新上传准备的共享接口及本轮相关增量已撤回；此前成果保留。见 [46 报告](46-附件共享接口阶段验收.md)。后续聚焦消息、工具及轨迹中的已有附件展示，不扩大范围。

## 已有附件图片呈现（2026-09-23）

qs-ui-attachment 已接消息和工具的持久图片，轨迹贡献等待宿主。全部 QS 86 文件 469 测试与逐文件四项覆盖率通过；全部 QS 浏览器 15 场景通过。见 [47 报告](47-已有附件图片呈现阶段验收.md)。普通文件附件、轨迹和原图完整交互仍待补齐，不关闭 C9。

## 文件附件与原图焦点（2026-09-23）

已有普通文件按官方 Chat 展示名称、类型、大小并保持与图片的顺序；原图补齐全屏 portal、初始焦点和焦点恢复。QS 471 项测试和逐文件四项覆盖率通过。见 [48 报告](48-文件附件与原图焦点验收.md)。轨迹宿主和其他第二优先待办保持未完成。

## 转写共享偏好与最新回归证据（2026-09-23）

后续实施记录位于 49–163 号阶段报告；上述早期“未交付”陈述对应当时阶段，不能用作当前完成矩阵。最近一次全体 QS 实测见 [162 报告](162-最终答案推理过程联动验证.md)：159 个文件、909 项测试通过，四项逐文件覆盖率均 100%。本批完成官方共享转写偏好、奇术通用设置入口、普通/紧凑成员呈现、无效控制行保护及最终答案推理联动，并验证真实 Host 持久化、刷新和 [跨界面两轮往返](163-转写偏好跨界面往返验收.md)。

这些是限定范围的最新验证证据，不是整个第二优先完成证明。原生查找、其余待核实业务分支、安全依赖可达性、性能门槛、全仓及最终 D/W 验收仍须逐项补齐；W11 按用户决定延期，仅保留说明入口，不接真实登录。
