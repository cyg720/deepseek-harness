# 目标领域错误传输修复

日期：2026-09-23。解决第 54 份记录确认的 Goal CAS 分类丢失；本阶段不代表 QS 目标界面或第二优先全部完成。

## 缺陷与修复

真实 Web Host 的过期 edit 请求在修复前返回 gateway/internal，回归断言明确失败，见 logs/repair-v1-goal-error-before.log。GoalError 原本仅继承 HarnessError，Gateway 的结构化 Remote 错误识别无法保留其领域代码。

GoalError 保留 HarnessError 基类、原错误名称和既有 GOAL_* 代码，实现官方 RemoteError 类型及识别字段；空 details 不携带额外服务状态。未修改 Gateway，未增加通用异常放行规则。目标错误类型迁入二开自有 src/qs/error-codes.ts，Host 原出口保留转导出，纯 Client 类型出口使用同一声明。现有模型工具依靠 HarnessError 提取错误元数据，不能通过替换基类破坏该行为。

## 行为验证

- 真实 Host 经 HTTP /api/goals 调用 edit、pause、resume、complete、clear，旧 ref 均返回 GOAL_STALE_REVISION；目标内容及 goal/change 序列保持较新版本。block 没有官方 Remote 导出，不构造不存在的远程接口。定向回归通过，其余 5 场景在该次运行中因名称筛选未执行，见 logs/repair-v1-goal-error-after.log。
- Goal 全组 7 文件、140 用例通过，包含命令、工具、目标服务与相关生命周期测试。修改的可执行源码 runtime.ts 为语句 5/5、函数 2/2、行 5/5；无可执行分支。domain.ts、types.ts、qs/error-codes.ts 是类型声明，不将零分支分母解释成业务分支已覆盖。见 logs/repair-v1-goal-error-coverage.log。
- Goal 包 TypeScript、Client 全量项目编译、Goal Host bundle 均通过。真实 Host 测试在重新构建后运行，避免验证旧产物；日志 repair-v1-goal-error-types.log、repair-v1-goal-error-client-types.log 位于 logs 子目录。
- 组合浏览器检查为 7 文件、22 用例全部通过，95.43 秒，见 logs/repair-v1-goal-error-browser.log。覆盖 QS 五份组合测试及官方目标条、目标指令历史两份测试。
- 后续检查同步了独立浏览器 fixture 的四类现有失败代码；Connection 全部 14 文件、163 用例通过，fixture 定向 36 用例通过。vitest.config.ts 既有排除列表不度量 fixture.ts，因此相应覆盖率报告为 0/0，不能称为 100%；本次没有修改排除列表或阈值。见 logs/repair-v1-goal-error-fixture-coverage.log、logs/repair-v1-goal-error-fixture.log。
- fixture 更新后再次通过 Client 全量编译并重建 Connection bundle；其官方目标浏览器检查另行复测，不把更新前的 22 用例结果冒充后续源码的完整复测。

## 最终工程检查

- fixture 重建后官方目标浏览器 2 文件、4 用例通过，11.69 秒，见 logs/repair-v1-goal-error-fixture-browser.log。
- 完整 lint（含 Host 构建）通过；Client 全量编译通过。独立 NodeNext 消费方检查确认 297 个 workspace 声明 API 可编译，见 logs/repair-v1-goal-error-lint-final.log、logs/repair-v1-goal-error-client-types-final.log、logs/repair-v1-goal-error-consumer-types.log。
- 完整 doc-sync 最终 34 通过、0 失败、0 跳过，54.93 秒，见 logs/repair-v1-goal-error-doc-sync-final.log。中间失败为事件关系图源码行号过期、随后中文引用及配对记录未同步，均已修正并由完整命令复测通过。
- 最终真实 Host 回归对错误信封使用完整字段相等断言，确认 details 确实为空，没有额外泄露字段；该定向用例通过，其余 5 项由过滤未运行，不计入这次通过数。代码差异格式检查通过。
- 官方清单共 159 个原文件，本批 15 项新增登记；路径有序、唯一，差异快照锚点全部有效。二开自有类型、浏览器测试、笔记和验收记录未混入官方清单。没有提交或推送。

## 仍需验收

QS 目标 dock、历史行、失败后保留草稿及版本冲突交互仍未交付；本次为其提供可判别的实际错误载荷，不标记 C15 完成。没有执行本轮全仓测试、全仓覆盖率或完整安全性能基准，不宣称第二优先整体通过。W11 继续延期，不接真实登录。官方原文件登记在第 38 批，二开文件不计入官方清单。
