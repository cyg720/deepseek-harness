# HTML 预览阶段验收

独立 HTML 子插件消费官方共享 prepareHtml，复用有限资源打包、相对读取与隔离引导代码，不复制安全算法。iframe 仅 sandbox="allow-scripts"，不提供同源、顶层导航或文件读取桥。每项 4 MiB、总量 32 MiB、最多 64 项限制仍由官方 packHtml 执行。

## 已执行

- 首批 QS 与官方 HTML 相关测试 47 条通过；扩大到官方预览及 QS 全包后 39 文件、291 条测试通过（覆盖率命令因未达门槛退出 1）。
- 控制异步顺序测试验证替换/卸载/标签取消后旧结果不发布，Blob URL 释放；读取和 Blob 创建失败不回显原始诊断。
- 真实浏览器 1 条通过，2 条筛选未运行：从文件树读取隔离工作区 HTML，关联 JS 修改标题、CSS 生效，预览脚本访问父页面被拒绝；既有文本/代码/图片流程仍通过。初次运行因测试变量 document 遮蔽浏览器全局导致最后几何断言失败，已修正变量名后重跑。
- 官方/QS 预览包构建及 Client 类型检查通过。
- 注册注入回归补充后 QS 预览包四项汇总：语句 96%、分支 87.30%、函数 98.30%、行 97.90%。仍未通过逐文件 100%：Preview.tsx、text/index.tsx；HTML 子插件及本次共享接口已经覆盖，不能宣称全包或全仓库验收通过。

## 剩余范围

PDF 正文，宿主资源故障与读取模式等分支补测，六类整体视觉/性能、安全边界完整验收，以及第二优先其余未交付功能。HTML 内浏览器外部网络能力沿用官方策略，本批没有额外收紧为离线沙箱。

日志见 logs/repair-v1-qs-preview-html-* 和 repair-v1-html-*；官方改动第 27 批。
