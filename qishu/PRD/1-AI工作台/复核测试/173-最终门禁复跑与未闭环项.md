# 最终门禁复跑与未闭环项

记录日期：2026-09-24；分支 `dev-workbensh`，HEAD `4904755062`，包含未提交工作区。状态：继续整改，不能整体验收。W11 已由用户明确延期，仅保留说明入口；真实登录不在范围内。

## 本轮实际结果

| 范围 | 命令 / 证据 | 结果 |
| --- | --- | --- |
| 完整 QS 真实浏览器 | `DSH_SNAPSHOT=replay pnpm exec vitest run --config vitest.web.config.ts apps/web/tests/qs` | 8 文件、41 项通过，205.10 秒；`logs/repair-v1-final-qs-web.log` |
| 长历史容量 | [171 测量卡](171-长历史浏览器测量卡.md) | 三档各 30 次、共 90 样本完成；5 万事件首次最新回答可见中位数 6.34 秒，尚未完成性能预算与基线比较 |
| 桌面 ZIP 防护 | [172 验证记录](172-桌面ZIP安全缓解验证.md) | 3 项真实 ZIP 用例、有效负向对照、实际运行时准备通过；依赖公告仍存在 |
| 全仓 lint | `pnpm run lint` 首轮失败；修复后 `pnpm run lint:contracts-ready` | Host 构建通过；初始两处 unbound-method 已消除，完整 lint 阶段退出 0；`logs/repair-v1-final-lint-corrected.log` |
| 共享 Hook 回归 | 官方共享呈现及 QS lifecycle 两个定向文件 | 2 项通过；`logs/repair-v1-shared-hook-lint-regression.log` |
| 最终类型 | `pnpm run typecheck` | 退出 0；`logs/repair-v1-shared-hook-types.log` |
| Hygiene | `pnpm run hygiene` | 14 门禁通过、2 失败；`logs/repair-v1-final-hygiene.log`；其中 package invariants 随后已修复并定向通过，Cordis 配置仍失败 |
| 文档 | ZIP 更新后的 `pnpm run doc-sync` | invariants 文案更新后复跑 34 项通过、0 失败、0 跳过，51.23 秒；`logs/repair-v1-invariants-docs.log` |

## 本轮修正

共享 `ChatPresentation.useSearchableHidden` 实际是无实例接收者的官方 Hook，二开接口改为只读函数属性，保持原函数身份和 Hook 调用约定；没有新增包装器或关闭 lint。文件位于官方目录的 `qs/`，属于二开自有文件，不登记为官方原文件。

三个 QS 包 agent-preset、model-selection、settings-models 已解释为何无独立 invariant，但英文句式不符合仓库机械门禁。将相同语义统一为 `No invariant companion is published because ...`，中文语义保持一致并重新记录配对。`pnpm run verify-package-invariants` 已通过，未添加空伴随插件，未放宽检查规则。

桌面 ZIP 防护涉及的四个官方原文件已登记第 98 批，中文说明、差异快照和总表同步；二开 helper、测试及 Agent Note 不混入官方原文件清单。

## 尚未闭环，不能计为通过

1. 全仓覆盖率的 57 项 Windows 符号链接权限失败仍待具备对应权限的环境验证；未修改系统权限、跳过测试或降低覆盖率门槛。QS 921 项四项 100% 的证据见 170，不等于全仓覆盖率通过。
2. ACP profile 的 `cordis.yml` 在 Git 索引为 mode 120000 符号链接，当前磁盘是 59 字节普通文件，内容为链接目标相对路径，因此 Loader 数组校验失败。这不是合法配置；也不能修改校验器让文本路径充当 Loader 配置。须恢复正确检出语义并重新验证。
3. 性能仍缺第一优先同负载基线、5 万事件打开耗时归因、连续流式输入、全量已加载 1k/10k 阅读节点及完整 W 面板资源测量。已完成采样不替代这些要求。
4. 两项 extract-zip 高危仅在直接调用点缓解，尚无安装依赖补丁；中危可达性和其他供应链余项仍需逐项收敛。
5. D1–D7、W1–W12 及 G/T 矩阵需最终逐条关联证据；真实浏览器原型全状态、原生查找、同页面连接服务替换、预设提供者热卸载等尚未全部证明。
6. 本轮 QS Web 通过不代表仓库全部 Web、快照或真实在线模型测试通过；无实际执行证据的范围继续标为待验。

没有提交或推送 Git。所有失败原始日志保留，不用修订后的成功日志覆盖失败证据。

补充核对：`git diff --check` 退出 0（仅既有 CRLF 归一提示）；官方原文件总表 183 条，路径唯一且按字典序排序。
