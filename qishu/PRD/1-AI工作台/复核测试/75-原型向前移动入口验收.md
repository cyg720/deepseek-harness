# 原型向前移动入口验收

日期：2026-09-23。按原型 `qishu/prototype/readiness.js` 的“向前移动”入口补充活动停靠标签重排，调用官方 placeTab 动作。界面沿用奇术本地化及样式，未复制官方页面。此次只改 QS 自有实现、测试及说明，没有新增官方原文件改动，未提交 Git。

## 行为与范围

活动标签向前移动一位，首标签、空栏及浮窗禁用。动作保持标签 id、资源信号及 pin 数量，顺序沿用共享布局保存及刷新恢复。按钮使用原生键盘激活。原型示意的是面板顺序，正式布局中对应栏内标签顺序；跨栏及整体面板顺序仍需另行实现和验收，不能将本按钮当作全部拖拽等价操作。

真实 Host Chromium 场景通过 Enter 激活按钮，断言实际顺序、活动内容和禁用状态；随后继续执行刷新、官方界面切换保存、窄屏浮窗、损坏记录过滤和退出清理。测试仍使用静态登录，不接真实认证。

## 验证

- QS 全部 107 文件、664 用例通过；QS 源码语句 4006/4006、分支 2978/2978、函数 1275/1275、行 2995/2995，四项 100%。证据：`logs/repair-v1-layout-move-coverage.log`。不代表全仓覆盖率。
- 本插件单独覆盖率首次运行暴露禁用按钮内重复判断的不可达路径，以及 Host 入口未被该局部测试范围执行；简化点击处理器并运行包含 Host 装配的全 QS 范围通过，没有降低门槛或添加覆盖率忽略。
- Host/Client 类型检查、定向 oxlint、QS 右栏构建通过，日志分别为 `repair-v1-layout-move-types.log`、`repair-v1-layout-move-lint.log`、`repair-v1-layout-move-build.log`。
- 新构建真实浏览器定向场景 1 个通过，11 个未选中场景跳过，耗时 11.16 秒：`logs/repair-v1-layout-move-browser.log`。
- README 与既有 Agent Note 中英配对更新；全工作区 diff whitespace 检查通过。文档门禁 34 项通过、0 失败、0 跳过，耗时 65.22 秒：`logs/repair-v1-layout-move-doc-sync.log`。

## 未完成

标签跨栏、面板顺序、浮窗移动及尺寸的键盘等价入口、菜单焦点及主题、原型完整视觉对照仍未关闭。T23 和第二优先整体目标继续保持未完成；没有以定向浏览器结果代替全量验收。
