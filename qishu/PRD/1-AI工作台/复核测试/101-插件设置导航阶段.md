# 插件设置导航阶段

日期：2026-09-23。新增 qs-ui-settings-plugins 的父分区及动态标签导航实现，未提交，尚未接入 Web 装配。

组件按官方职责注册 qs.settings.plugins.tab。首次访问标签才挂载其页面，已访问页面隐藏时保持挂载；移除贡献释放对应页面，当前标签不可用时回退至第一个可用标签。方向键、Home、End 支持焦点和选择同步。槽目录和语言版本提供稳定快照；卸载释放父子槽及本包词典。

两个测试文件、两个用例通过，四项覆盖率 100%（语句 61、分支 25、函数 23、行 38）。测试包含真实 DOM 草稿保持、键盘导航、贡献移除、父槽迟到、目录排序、语言变化及卸载重装；新包 TypeScript 项目编译通过。lint 已修正非空断言后重验，日志见 logs/repair-v1-plugin-navigation-*.log。README 已补齐双语；依赖锁登记在第 62 批。

Shell、AgentLoop、SubagentModelSelection、WebSearch 配置卡、只读清单、原型两入口映射及真实浏览器装配尚未完成。当前不把空态视为配置功能，不将本包标为已交付；完整目录生成与根编译聚合需随装配接线执行，第二优先目标仍未完成。
