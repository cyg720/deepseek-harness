# 运行中待答请求与整族重载复核

日期：2026-09-24。分支 dev-workbensh；HEAD 4904755062702eaadf19c5897ab86bc6988716e9，证据包含未提交工作区。范围为 T16/T32/T42 的运行中审批与提问重载，补充报告190的普通草稿场景。

## 验证方式

扩展现有[真实 Host 回归](../../../../apps/web/tests/qs/interaction-roundtrip.e2e.ts)中的 approval-composer/reject 与 question-composer/answer。使用原有正式 Web Loader、实际客户端产物、官方工具及持久会话事件；外部模型沿用合法 Session 回放。通过受控 Promise 延迟首次插件构建通知，在真实请求等待用户回答、轨迹视图打开后，按实际模块图发送两轮 QS rebuilt 通知，每轮外壳最后重载。

测试观察真实外壳样式的两次插入作为重载完成信号，不以固定 sleep 猜测结束。外壳按 fixture 配置回到官方默认页面后，在同一文档切回 QS 并选择原会话。该步骤不是页面刷新，也不模拟 Host 请求或直接调用卡片回调。

## 断言与结果

1. 重载后当前会话只有一个可回答卡片；重载期间 approval/decided 和 tool/result 均为零。重载不能暗中拒绝、回答或完成工具。
2. 普通 composer 草稿完整保留，未自动发送。
3. 提问草稿由 qs-questions 插件内存持有，插件替换时按其 README 约定清空；测试明确断言文本为空、Blue 未选中，再重新选择与输入。不将该已知限制描述为回答草稿已恢复，也未新增持久化秘密或用户答案。
4. 提问提交后真实工具结果严格等于 Blue 与 Include accessibility notes 的答案载荷，结果仅一个；提交完成后焦点回到当前输入。
5. 审批拒绝只产生一个 rejected 决定，真实目标文件不存在；不把回放的 DONE 当作文件执行证据。
6. 两条场景继续验证轨迹视图中的唯一交互、官方/QS 往返、待答卡 ARIA、窄屏输入与阅读区域以及最终结果。

Windows：2 项通过、19 项筛选未执行，18.38 秒，退出码 0；日志 logs/repair-v1-pending-hmr.log。

Linux：专用 qs-review-20260924-sdk 容器 /review，同步本次测试源码后执行同一筛选；2 项通过、19 项筛选未执行，16.54 秒，退出码 0；日志 logs/repair-v1-pending-hmr-linux.log。拒绝审批不需要执行已知受沙箱条件阻塞的允许写入路径，故此结果不解除 Linux 允许审批真实执行的环境阻塞。

Host TypeScript 构建和定向 lint 通过，日志 logs/repair-v1-pending-hmr-types.log、logs/repair-v1-pending-hmr-lint.log。上一轮完整 Windows QS Web 53 项通过仍见报告191，本轮只重跑本次修改的两个真实场景，不把它们拼成一次新的完整执行。

文档门禁 doc-sync：34 通过、0 失败、0 跳过，54.20 秒；见 logs/repair-v1-pending-hmr-docs.log。相关 git diff --check 通过。

## 范围限制

本轮验证当前配置实际装配的 QS 插件重载，不新增插件或服务。带可选 schedule 的完整34包跨平台并集另由报告190验证；本轮没有将默认配置冒称全部可选插件。HMR rebuilt 不等于模块图热删除。重载期间的瞬时键盘焦点、用户已按提交但 RPC 尚未完成的精确时序、真实原生目录对话框及全部 D/W 视觉状态仍不在本证据内。

更改仅为 QS 自有测试与复核文档，不涉及官方原文件，不新增官方源码改动记录。本轮不提交 Git。
