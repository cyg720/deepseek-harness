# 预设设置 Web 装配与真实保存验收

日期：2026-09-23。独立 qs-ui-agent-preset 已装配 Web，默认预设与选择器策略通过真实 Host 浏览器验证；新会话选择与空白会话同步等仍未完成，第二优先整体未验收。未提交 Git。

## 浏览器证据

用 dsh 隔离 profile 启动真实 Host，读取内置预设目录，关闭选择器后观察默认下拉禁用；页面刷新后仍为关闭。重新开启后，默认预设从 standard 改为 ptc，Host 回执及刷新恢复均确认该值。奇术切到官方再返回后仍可打开预设设置并读取保存值，pageerror 为空。

请求断言校验 agent-presets 命名空间、modeSelectionEnabled 布尔字段及 expectedRevision 类型；默认值请求只写 default，Host 回执确认 ptc。新增 QS persisted preset settings 快照保留界面实际文本，Host 提供的中文预设名称不会被英文界面错误改写。

首轮 setChecked 要求点击后立即更新，与受控控件等待 Host 回执的行为不一致而失败；改用点击、真实响应及最终状态断言。开关的响应等待与点击使用 Promise.all，避免操作失败后遗留未处理拒绝。没有弱化持久化断言或修改产品权限规则。

## 已执行检查

| 检查 | 结果 |
| --- | --- |
| 离线依赖安装及 postinstall | 成功退出 |
| 全部 QS 单元测试 | 140 文件、755 用例通过 |
| QS 源码覆盖率 | 语句 5535/5535、分支 4083/4083、函数 1753/1753、行 3953/3953，均 100% |
| 新增真实浏览器场景 | 1 通过、16 未选中；新增 1 个界面快照 |
| 根级 Host/Client 类型检查 | 通过 |
| 浏览器测试定向 lint | 通过 |
| doc-sync | 34 项通过、0 失败，78.98 秒 |
| 定向 diff --check | 通过 |

日志为 logs/repair-v1-preset-web-*.log。QS 单元测试范围不含官方命令包，与 119 报告的联合测试数量不可直接比较。

## 剩余范围与登记

尚需新会话预设选择、当前会话预设标识、空白会话与策略变更同步。其余 W10 功能、其他 D/W 验收、安全检查、性能基准及最终仓库全量检查保持未完成状态；不接真实登录，W11 延期说明不变。

官方三个既有文件更新见[74-预设Web装配登记](../../../官方源码改动记录清单/74-预设Web装配登记.md)。未改动官方预设 Host 实现，二开源文件和 qs 浏览器测试不计入官方原文件清单。
