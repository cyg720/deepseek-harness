# 子代理模型配置目录阶段

日期：2026-09-23。此阶段仅完成子代理配置卡依赖的目录读取和候选路由合并，不代表配置卡已可用或第二优先验收完成。

## 实现

新增 QS 包内部 model-catalog.ts，对照官方 subagent-model-selection-card-controller 的后发请求、连接代际、已消失路由保留规则。消费现有 session.modelCatalog RPC，不新增 Host 接口、服务或插件。读取失败不暴露错误原文，供应商部分失败保留成功结果，reset 清空旧连接目录，dispose 释放监听并阻止迟到结果采用。

路由候选按 provider/model 精确匹配，实时目录之外的已保存和草稿路由保留为不可用候选，允许后续配置卡移除。标识使用 JSON 二元组，不解析业务字段，避免分隔字符碰撞。

## 验证与限制

- 新增三个用例，覆盖可控请求逆序、旧连接拒绝、卸载迟到成功、部分失败、明确拒绝及重试成功，以及路由保留、去重与标识碰撞。
- 配置包覆盖率曾执行通过：249 语句、141 分支、79 函数、154 行均 100%；随后测试夹具补齐官方要求的 default/routableProviders，并通过全仓 Host/Client 类型检查和新增文件静态检查。最终测试重跑结果见 logs/repair-v1-model-catalog-coverage.log。
- 新文件位于 packages/qs，无官方原文件改动。本阶段尚未注册配置卡，也未接入适配器更新、设置更新及连接重置事件；下一步需完成卡片呈现、原子保存和真实浏览器验证，不能将读取器测试代替完整功能验收。
