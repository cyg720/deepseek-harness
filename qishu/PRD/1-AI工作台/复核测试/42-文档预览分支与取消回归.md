# 文档预览分支与取消回归

日期：2026-09-23。接续 41 报告中的覆盖率缺口；本轮只改 QS 自有代码、测试和说明，没有新增官方原文件改动。

## 本轮完成

补充 PDF 可见区域观察、旧 worker 迟到失败、文档替换、已关闭标签、迟到画布完成/拒绝、独立子插件注入。补充文本目标行、空文件、字节正文拒绝、缺失资源、元数据错误、二进制重新读取及切回分页、未注册正文、嵌套滚动、底部翻页、初始失败重试、源行尚未挂载及超出文件末尾导航、无会话资源地址拒绝。

竞态使用可控 Promise 和显式观察器回调，不使用固定延迟。观察器全局替身在每例后恢复。测试中的字节返回按官方 readAll 协议传 base64，worker 失败使用官方 PdfWorkerFailure 和原生 ErrorEvent。

生产代码删去两处不可能出现的兜底：已取得 content 的正文必有当前标签状态，直接使用状态中的 wrap；PDF 页面宿主无条件挂载，effect 在 ref 提交后订阅观察器，沿用官方 ref 写法。没有新增行为、降覆盖率门槛、排除源码或伪造测试状态。

## 实际验证

| 范围 | 结果 | 证据 |
|---|---|---|
| 预览包覆盖率 | 7 文件、41 测试通过，四项逐文件 100% | logs/repair-v1-qs-preview-branches.log |
| 全部 QS 包覆盖率 | 81 文件、451 测试通过，语句 2976/2976、分支 2146/2146、函数 939/939、行 2268/2268 | logs/repair-v1-preview-all-qs-coverage.log |
| Client 类型检查 | 通过 | 实际执行 tsc -p tsconfig.client.json --noEmit |
| 预览包 lint | 通过 | 实际执行 scripts/run-oxlint.ts packages/qs/qs-ui-sidebar-documentpreview |
| 文档快速门禁 | 16 项通过 | logs/repair-v1-preview-branches-docs.log |
| QS 预览 bundle | 通过 | logs/repair-v1-preview-branches-build.log |
| 真实 Web 预览组合场景 | 1 通过，2 个不匹配名称的场景跳过；两页 PDF 像素、HTML 隔离、图片、文本与侧栏回归通过 | logs/repair-v1-preview-branches-browser.log |

全部 QS 覆盖率命令：`pnpm run test -- packages/qs --coverage --coverage.include='packages/qs/*/src/**/*.ts' --coverage.include='packages/qs/*/src/**/*.tsx'`。不包含仓库其他包覆盖率，不能扩称全仓通过。日志中的资源地址异常来自预期拒绝用例；测试断言异常文本且确认没有 Host 请求，并非遗漏的运行失败。

## 仍需完成

C9/W1 的附件、可交付物和完整引用交互尚未接入；W3 目录选择、W4 标签刷新恢复及完整键盘路径，以及作业、调度、目标、子代理、轨迹、设置等仍按原计划推进。W11 已明确延期。当前统计不能证明这些未实现功能完成；全仓门禁、安全及性能总验收仍待执行。
