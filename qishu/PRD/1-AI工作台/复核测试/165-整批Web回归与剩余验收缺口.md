# 整批 Web 回归与剩余验收缺口

日期：2026-09-23。对象：dev-workbensh 当前未提交工作树。本记录不将历史阶段通过数作为第二优先整体完成证明。

## 本批回归修正

执行 `DSH_SNAPSHOT=replay` 下的 `node node_modules/vitest/vitest.mjs run --config vitest.web.config.ts apps/web/tests/qs`，首轮 8 个文件中 7 个通过、1 个失败，39 项中 38 项通过、1 项失败。日志为 `logs/repair-v1-final-qs-browser.log`。该目录包含 jsdom 装配场景，因此 39 项不能全部称为真实 Chromium 测试。

失败位于 `apps/web/tests/qs/transcript-disclosures.e2e.ts`：旧断言要求收起时上下文节点数为零，与已实施的可搜索折叠保留 DOM 的行为冲突。修正为统计 Chromium `checkVisibility`，同时断言节点数量保持、收起不可见、键盘展开可见、再次收起及展开仍有效。保留桌面与窄屏的溢出检查、系统提示详情高度限制、原有收尾快照及关闭默认值，未更新快照或删除展示边界断言。

修正后该文件 1 项通过，快照 replay 通过；定向 Oxlint 和 `git diff --check` 通过。日志为 `logs/repair-v1-final-disclosure-visibility.log`。此文件属于 QS 自有测试，不列入官方原文件改动清单。

随后以同一完整命令复跑，8 个文件、39 项全部通过，耗时 182.65 秒，无失败或跳过；日志为 `logs/repair-v1-final-qs-browser-verified.log`。`pnpm run test:docs` 为 16 项通过、0 失败、0 跳过，见 `logs/repair-v1-final-web-docs.log`。这些结果证明当前已有 QS Web 用例通过，不代表尚未编写的 G07 或性能场景已验收。

## 安全扫描补充分流

全依赖扫描为 2 high、17 moderate、3 low、0 critical；`pnpm audit --prod --json` 为 0 high、12 moderate、1 low、0 critical，两条命令均因存在公告退出 1。日志分别为 `logs/repair-v1-security-refresh.json` 和 `logs/repair-v1-security-production.json`。这里统计的是公告命中，不是已证实可利用漏洞数量。

两条高危均来自桌面开发依赖 extract-zip 2.0.1，源码调用位于 `apps/desktop/scripts/prepare-runtime.ts`：固定 nodejs.org 发布地址、固定 Node 版本，比较归档 SHA256 与 SHASUMS256.txt 后解压。不是 Web 用户上传的任意 ZIP 入口，但本机下载缓存和供应链仍属于风险面，不能因 devDependencies 或校验存在就标为修复。之前补丁及不可安装修复版本的结论见 [34](34-依赖安全补丁与可达性复核.md)。

生产扫描还包含工作区 test-support 包对 Vitest 的 dependencies 引用；生产依赖分类不能替代应用可达性分析。MCP 源码使用 SDK 的 Client、StdioClientTransport 和 StreamableHTTPClientTransport；针对源码的检索未发现直接启用 Hono/Express 服务或解析用户 .proto 的路径，但尚未完成传递依赖内部逐路径审查，不宣称中低危全部不可达。

## 已确认的剩余工作

G07 任务通知消费者仍缺失：qs-shell 提供 qsToast 展示服务，qs-ui-jobs 读取 jobsBySession 和控制流状态，但当前 QS 源码没有将权威任务终态转换送入 toast 的消费者。计划 03/C10、09/G07、10/P-B05 要求已打开可访问会话的已观察转换才通知、初始和重连快照不重放、Host/退出隔离、去重有界。现有作业列表和反馈表单不能替代此能力，下一批需补实现及 T39 验收。

性能要求尚未证明：05 要求至少 5 次探索中位数，需要 p95 判断时至少 100 次；12/M0 要求 1k/10k/50k 合成事件及至少 30 次 production 浏览器样本。已有组件 SSR 测量不覆盖首屏、输入、分页、切视图、内存与卸载，不能用于放行。功能逐项核验、全仓最终检查、原生查找栏命中折叠内容仍须完成。

W11 按用户决定延期，仅保留说明入口；不接真实登录。本批未提交或推送 Git，整体目标保持未完成。
