# 供应商配置与凭据分步保存

日期：2026-09-23。范围：`packages/qs/qs-ui-settings-models` 的编辑器保存基础逻辑。尚未接入编辑界面或正式 Web，不能据此验收供应商编辑功能或完整 W10。

## 实现与官方对应

参照官方 `packages/client/ui-settings-models/src/client/ProviderEditor.tsx` 的配置先提交、凭据后保存语义。`provider-save.ts` 调用共享服务提供的 `ModelsOperations`，没有新增 Host 接口或配置权威状态。配置冲突或拒绝时不写凭据；配置成功先把完整确认视图交还编辑器，凭据失败保留配置成功结果。调用方重试必须按确认后的 user 和 revision 重算字段差异；保存器不会自行推断草稿已提交。

单个编辑器只允许一个在途保存，调用时复制操作与凭据。卸载后不再采纳视图、不发起后续凭据写入；已发送的请求可能已在 Host 生效，回执标记失活不代表回滚。提交确认后若凭据引用改变，禁止向旧引用写入。错误结果不包含原始诊断或凭据内容。凭据单独提交仍由官方 credentials 操作授权，不能用设置只读状态替代凭据权限。

## 执行验证

- 保存器新增 11 个确定性测试：部分成功及仅密钥重试、配置冲突和拒绝、只读、引用改变、重复保存、调用方草稿变化、配置/凭据在途卸载、传输异常及恢复。
- 新包 3 个测试文件、16 个用例通过。限定新包覆盖率：语句 75/75、分支 65/65、函数 20/20、行 48/48，均为 100%。日志 `logs/repair-v1-provider-save-coverage.log`。
- 根级 Host/Client TypeScript 增量编译通过。定向 lint 初次发现测试箭头参数括号格式，修正后通过，日志 `logs/repair-v1-provider-save-lint.log`。
- 双语 README 与已有架构记录同步；doc-sync 34 项通过、0 失败、0 跳过，用时 54.01 秒。日志 `logs/repair-v1-provider-save-doc-sync.log`。
- 相关已跟踪文件 `git diff --check` 通过。本轮未修改官方原文件，未提交。

## 仍需完成

保存器尚无产品编辑器调用。供应商字段编辑、配置差异生成与 schema 校验、密钥输入验证、只写输入清理、发现/删除/引导、插件正式装配及真实浏览器保存验收仍未完成。当前自动化仅证明保存器的确定性行为，不证明真实 Host 持久化、整体 QS 覆盖率、安全性能或全仓验收。后续必须验证用户可见的部分失败提示与重试确实只写未完成部分。
