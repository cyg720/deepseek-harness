# 输入区字号偏好联动

日期：2026-09-23。局部显示修复，未提交；第二优先整体尚未完成。

## 核查结论

完整源码中的 700px 媒体查询存在。先前根据文件尾部片段作出的“缺少媒体查询、移动端字号泄漏到桌面”判断不成立；真实浏览器的 700px／701px 断点断言通过。没有据此补入重复媒体查询。

真实问题是输入框固定为 13px，忽略官方 --dsh-content-font-size 变量。现在桌面跟随该变量并保持小 1px 的差值，缺少变量时仍为 13px；视口宽度不超过 700px 时最低 16px，避免移动端聚焦放大。没有新建偏好 store，也未改变发送行为。

## 测试证据

- 新增实际计算字号断言，旧实现遇到官方变量 17px 时返回 13px 而非预期 16px，测试失败，证实回归可检测问题。
- 更新输入区 CSS 并重新构建本包后，真实浏览器设置场景通过：桌面默认 13px、700px 为 16px、701px 恢复 13px；变量 17px 时输入为 16px，变量 12px 时桌面为 11px、窄屏仍为 16px。
- 同一场景继续覆盖设置弹窗焦点、草稿保留、语言写入 Host 回执及刷新持久化。1 个场景通过，14 个未选中；本轮没有宣称全仓测试通过。
- Host/Client 类型检查、输入区构建、浏览器测试文件 lint、README 双语配对更新均以退出码 0 完成。
- 日志：logs/repair-v1-composer-font-{red,green,build,types,lint,pairing}.log。另有 breakpoint-red.log，其中断点验证实际通过，文件名不代表测试失败。

字号变量由浏览器用例直接设置并恢复，此证据只证明 CSS 消费联动；主题／字号真实设置入口及其持久写入仍待独立 qs-ui-theme 实现验收。消息正文的字号消费亦待配套核查。

## 文件与范围

本轮仅变更二开输入区样式、包 README 双语配对、已有 apps/web/tests/qs/interaction-roundtrip.e2e.ts 及本报告。测试属于二开 qs 子目录，不是官方原文件；按登记规则不加入官方源码改动清单。本次是局部 CSS 消费修复，不改变插件职责或生命周期。
