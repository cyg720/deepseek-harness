# 安全还原评估与验证

2026-09-18 针对原标否的八个文件，按当前实现是否可以安全回退重新判断；用户明确排除 .gitignore。结果为一项还原、七项保留。本批不引入独立 bundle、profile 或新启动方式，不修改产品运行代码。

## 已实施还原

scripts/verify-package-readme-model-experience.ts 的七包白名单是唯一相对 be7706c54e 的差异，移除后与该基线逐字节相同。还原包含框架提交中的白名单，不能只恢复 HEAD 后留下已提交的二开特例。

七个自有包 README 中的 Model Experience 改用检查器已有的完整分节：模型可见内容、Token 影响、KV 缓存影响；同步中英文和配对记录。修改只涉及文档，未改变插件注册、运行依赖、用户交互或 UI。官方 Model Experience 检查实际执行通过，275 个 README 全部符合规则。

## 保留依据

Web bundle 仍直接注册七包且默认 workbench，manifest 仍是插件解析所有者。因此还原装配或依赖会影响当前工作台；还原官方测试的界面固定会改变测试实际启动的 UI，并丢失开发者切换配置通路。独立装配仅是上一轮提出的替代方案，尚未实现，不能当作已经具备的回退条件。

第 48 项只改变测试超时而不改变功能；[既有全量验证记录](02-复核修复与全量验证.md)已记录多仓库 Git 用例在 5000ms 下全仓并发超时、独立复跑通过。为避免重新引入已知故障，保留局部 30 秒预算，不以单次轻负载测试为删除修复的依据。

## 验证

| 检查 | 实际结果 |
| --- | --- |
| 还原文件与 be7706c54e 字节比较 | 完全相同，未留下七包特例 |
| `node --import tsx/esm scripts/verify-package-readme-model-experience.ts` | 275 个 README 全部通过 |
| `node node_modules/vitest/vitest.mjs run packages/qs` | 21 个文件、125 个用例通过 |
| `npm.cmd run doc-sync` | 34 项通过，0 失败 |
| `node --import tsx/esm scripts/run-oxlint.ts .` | 退出码 0 |
| `git diff --check` | 通过，既有原型文件有 CRLF 转 LF 提示 |
| 总表与累计差异 | 51 项保留差异、1 项已还原历史，数量一致 |

本批未修改运行源码，不重复启动浏览器或重跑产品全量测试；不能将此次单元与文档检查表述为新的全量验收。处置明细见 [官方文件登记](../../../官方源码改动记录清单/07-安全还原与保留登记.md)。未提交或推送。
