# 数值配置卡交互回归阶段

当前范围为 Shell 与 AgentLoop 数值配置卡；本记录不代表第二优先已完成，也不代表全仓验收通过。

## 已验证

- 新增 `packages/qs/qs-ui-settings-plugins/tests/numeric.client.spec.tsx`，实际断言保存操作数组和最初编辑版本。镜像版本更新不会静默改变未保存草稿的提交版本；冲突后只有显式采用当前版本才使用新版本提交。
- 非法数值阻止提交；恢复继承发送 `unset`，不是把基础值写成用户覆盖；写入拒绝保留草稿；放弃恢复镜像值；只读禁用输入。
- 可控 Promise 验证提交期间不重复发送，卸载释放提交器，旧实例迟到成功不在新实例显示成功状态。
- 更新生命周期测试依赖与扩展卡名称，验证两个真实配置卡注册后第三方贡献仍可独立增删。
- 包测试命令 `node node_modules/vitest/vitest.mjs run packages/qs/qs-ui-settings-plugins`：5 个文件、10 个用例通过。日志：`logs/repair-v1-numeric-package.log`。

## 待验证

数值卡真实 Host 浏览器保存、生成目录同步、覆盖率缺口及相关文档门禁仍需逐项完成。旧浏览器场景的“无配置卡”断言已不适用，必须改为真实卡片及写入回执验证，不能沿用历史结果宣称通过。WebSearch、SubagentModelSelection 及其他第二优先剩余项不在本次通过结论内。

全仓 Host/Client 类型检查已通过。随后调整 JSON 数字展示并修复静态检查发现的格式问题，包测试重跑仍为 5 个文件、10 个用例通过，包级 oxlint 通过。中英文 README 配对记录已更新，配对校验仍需运行。类型、静态检查和配对记录日志分别为 `logs/repair-v1-numeric-root-types.log`、`logs/repair-v1-numeric-lint.log`、`logs/repair-v1-numeric-pairing.log`。
