# 模型重试真实回放与 QS 覆盖率复核

日期：2026-09-24。基线为 dev-workbensh / 4904755062702eaadf19c5897ab86bc6988716e9 加当前未提交工作区。本轮补齐 D3/T04 的真实重试闭环，并更新全 QS 覆盖率证据；没有更改官方源码或插件运行行为。

## 重试业务验证

新增[QS 真实浏览器测试](../../../../apps/web/tests/qs/transcript-disclosures.e2e.ts)，参考[官方场景](../../../../apps/web/tests/live-interactions.e2e.ts)，使用同一已提交 live-interactions Session fixture。外部模型通过官方 replay override 注入 SERVER 错误，重试、持久事件、投影、Loader、UI 和停止操作均保持真实。

| 场景 | Host 证据 | QS 界面与历史证据 |
| --- | --- | --- |
| 首次失败后恢复 | 一个 llm/retry、一个对应 retryId 的 llm/retry-started，唯一 turn/end 为 completed | 成功正文可见；展开过程仍有重试记录和真实错误原因；刷新后诊断 ARIA 相同。 |
| 重试耗尽 | 两个 llm/retry 与逐项匹配的 started，唯一 turn/end 为 error | 第二次重试记录与独立失败行并存，失败原因包含 upstream 503；没有工具重放按钮。 |
| 等待中取消 | 一个 llm/retry、零 started，唯一 turn/end 为 aborted | 服务等待态可见后通过真实停止按钮取消；诊断显示已取消；刷新不新增重试或轮次。 |

取消场景将官方可配置退避设为 60 秒，在等待文案实际可见后立即停止；不使用固定 sleep、假时钟或修改重试服务。恢复和耗尽使用 25 毫秒无抖动退避。最终诊断含错误代码与原因，三个状态分别保存 owner-local ARIA 快照；刷新后重新展开，与实时结束状态逐字比较。

首次执行：恢复/耗尽通过，取消在 teardown 被“夹具未全部消费”拒绝。原因是测试为取消路径额外配置了一次成功调用，而正确行为不会启动该调用。将取消脚本改为只有首次失败后再次执行，保持官方消费校验，不使用跳过或忽略异常。

最终 Windows：3 项通过，17.33 秒，退出码 0；logs/repair-v1-retry-lifecycle-history.log。Linux：同步相同测试与快照至专用容器 /review，3 项通过，14.43 秒，退出码 0；logs/repair-v1-retry-lifecycle-linux.log。Host 类型构建及定向 lint 通过，分别见 logs/repair-v1-retry-lifecycle-types.log、logs/repair-v1-retry-lifecycle-lint-final.log。

## 当前 QS 覆盖率

执行命令：`node node_modules/vitest/vitest.mjs run packages/qs --coverage --coverage.include=packages/qs/*/src/**/*.{ts,tsx} --coverage.reportsDirectory=logs/qs-current-coverage`（PowerShell 实际对 glob 参数加引号）。

161 个测试文件、934 项测试全部通过，20.84 秒，退出码 0。语句 6637/6637、分支 4996/4996、函数 2031/2031、行 4647/4647 均为 100%，原逐文件门槛下没有未达标文件。日志 logs/repair-v1-current-qs-coverage.log，覆盖率产物保存在忽略目录 logs/qs-current-coverage。

覆盖率执行在本轮仅新增 Web 测试前，之后未修改任何 QS src 或其单元测试；因此覆盖的生产源码与本轮最终状态相同。它是全 QS 门禁，不是全仓覆盖率，也不覆盖浏览器测试自身。

文档门禁 doc-sync：34 通过、0 失败、0 跳过，52.64 秒；日志 logs/repair-v1-retry-lifecycle-docs.log。相关 diff 及新增文件尾换行、尾空格检查通过。

## T05 输出达到上限增量验收

在同一测试文件增加 max-tokens 场景，参考官方 agent-loop 的 StreamChunk 结束语义，仅以 replay override 控制外部模型输出。真实轮次结束原因严格为 max-tokens；截断前正文恰好显示一次，独立上限诊断可见，没有 turn-error 或 model-retry 行，输入可用且停止按钮消失。诊断没有自动继续按钮。页面刷新后正文和 ARIA 均一致，持久事件仍只有一个助手消息、一个轮次结束、零重试。回放脚本只允许一次模型调用，额外自动调用不能被第二段预置成功内容掩盖。

四条场景组合回归：Windows 4 项全部通过，20.89 秒，见 logs/repair-v1-output-limit-all.log；Linux 4 项全部通过，17.40 秒，见 logs/repair-v1-output-limit-linux.log。新增 QS output limit diagnosis 快照；Host 类型构建及定向 lint 通过，见 logs/repair-v1-output-limit-types.log、logs/repair-v1-output-limit-lint.log。T05 增量的 test:docs 为16通过、0失败、0跳过，17.60秒，见 logs/repair-v1-output-limit-docs.log；相关 diff 空白检查通过。没有新增或修改官方原文件，不新增官方源码改动登记；没有提交 Git。

## 验收限制

T04 的恢复、耗尽、等待取消和持久历史已获得真实回放证据；这不是外部线上模型网络请求测试。取消场景证明等待期间未开始第二次调用，不宣称浏览器关闭、进程崩溃、网络断开均已覆盖。T05 已有上述文本截断证据，但不推论全部多步骤工具截断组合均已验证；其他 D/W 状态仍按各自证据验收，不由本轮覆盖率代替。


后续静态复核修正：报告194发现新独立 Web 文件未进入 Host 编译清单，早期 Host 命令的退出0不证明该文件类型已覆盖。四条场景和快照已并入现有 Host 诊断文件 transcript-disclosures.e2e.ts；本报告浏览器历史结果保留，最终编译与合并后回归以报告194为准。
