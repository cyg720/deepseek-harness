# 轨迹请求诊断详情

日期：2026-09-23。补齐压缩请求和请求错误的已记录详情，未提交。

## 实现

请求详情继续默认折叠，展开后按官方 RequestView 显示压缩 summary 与 rawOutput，并分别标注摘要和原始输出；没有记录时使用缺窗说明，不用摘要伪造原始输出。内容块复用台账的安全文本与附件呈现，未知类型保留限制说明。

失败请求展开后显示错误代码与已记录错误文本。AUTH 依照官方 ui-trajectory 的 requestErrorMessage 行为，使用本地化认证配置提示，不回显供应商原始认证响应。错误字符串、原始输出和文件名都作为 React 文本，不能执行 HTML。

## 验证

- 轨迹包 3 个文件、12 个测试通过；语句 164/164、分支 147/147、函数 54/54、行 127/127，四项 100%。覆盖仅摘要、仅原始输出、两者都有、缺失、普通错误、仅错误代码、AUTH 原文不回显与标记转义。
- 真实浏览器从已持久化手动压缩历史进入轨迹，展开对应请求，核对摘要、原始文本和思考记录并保存详情快照；默认折叠时不挂载原始输出。1 个场景通过，另一个未选中。
- 类型检查、lint、构建和双语配对通过。未在本轮重跑完整 QS 或全部浏览器测试；不沿用此前数字声称新检查。
- doc-sync 34 项通过，0 失败，69.80 秒。
- 日志：logs/repair-v1-trajectory-diagnostics-*.log。

本轮无官方原文件改动。流式 partial、运行中工具、其他诊断节点以及长历史性能仍未完成；现有官方 TrajectorySnapshot 已提供 partial 和 runningCalls，后续应继续消费同一投影，不维护第二个生成状态机。
