# 源码装配残留清理与剩余权限失败

日期：2026-09-23。范围：169 记录中的剩余失败。本阶段修复源码装配和滚动条令牌，第二优先整体仍未验收完成。

## 实际修复

`tsconfig.base.json` 补齐 20 个已存在 QS 插件的 `/client` 源码入口。官方组合测试按正式 Web 插件清单动态加载 Client，缺少这些别名时无法找到 sidebar-files 等二开插件；别名只指向对应插件的真实源码，不复制服务，不改装配数量。官方原文件同批登记在 [96 登记](../../../官方源码改动记录清单/96-QS客户端源码别名登记.md)。

清理 60 个误生成的源码旁产物：connection 24 个、typert registry 12 个、file-upload 8 个、fs 8 个、agent-default-model 4 个、skill 4 个。逐个核实未被 Git 跟踪、有同名 `.ts/.tsx` 源码、修改时间为 2026-09-23 22:18，验证绝对路径位于当前仓库 packages 内后逐文件删除。没有递归删除目录，没有删除官方原源码。清单包含路径、对应源码及原时间，保留于 `logs/repair-v1-source-residue-inventory.json`。

清理后上传 Agent 所有权、Skill 作用域及文件服务定向测试恢复通过；生产 `assertAgentScope` 未修改，上传测试夹具和断言也未修改。这支持残留产物造成源程序与构建程序混用的诊断，不支持删除所有权校验的做法。

调度菜单使用官方成对浮层滚动条令牌，再在菜单内映射至原有奇术颜色。奇术配色和页面结构不变，源码增加局部中文注释；该文件为 QS 自有文件，不纳入官方清单。

## 已执行验证

| 范围 | 实际结果 | 日志 |
| --- | --- | --- |
| 官方设置页组合装配 | 2 文件、15 项通过 | `logs/repair-v1-qs-client-aliases.log` |
| 上传、Skill、文件服务 | 4 文件、72 项通过 | `logs/repair-v1-source-residue-regression.log` |
| 上批 34 个失败文件复跑 | 25 文件失败、9 文件通过；58 项失败、950 项通过、7 项跳过；未报告未处理错误 | `logs/repair-v1-source-alias-residue-rerun.log` |
| 滚动条令牌及 QS 调度 | 3 文件、37 项通过 | `logs/repair-v1-schedule-scrollbar-tokens.log` |
| 完整 QS 覆盖率 | 161 文件、921 项通过；四项逐文件覆盖率 100% | `logs/repair-v1-source-clean-qs-coverage.log` |
| QS 调度构建 | bundle 命令退出 0 | `logs/repair-v1-schedule-token-build.log` |
| 新构建调度浏览器回归 | 选中的真实日程用例通过，其余未选择 | `logs/repair-v1-schedule-token-browser.log` |

本轮 QS 覆盖计数：Statements 6618/6618、Branches 4988/4988、Functions 2024/2024、Lines 4634/4634。工具链固定为 Vite 8.0.16 后，以本次计数为当前证据，不沿用之前 Vite 6 运行的分母。

正式 `pnpm run typecheck` 退出 0，见 `logs/repair-v1-client-alias-types.log`；结束后复查清理清单，60 个产物均未重新生成。`pnpm run verify-tsconfig-paths` 确认别名有效，相关 `git diff --check` 通过。完整 `pnpm run doc-sync` 退出 0，见 `logs/repair-v1-source-clean-docs.log`。调度浏览器本次实际为 1 项通过、7 项未选择，不将后者计为通过。

## 尚未通过的条件

34 文件复跑的 58 项失败逐项查看错误：57 项为创建符号链接时 Windows 返回 EPERM，另外 1 项是调度滚动条令牌断言；后者已修复并定向通过。没有再把原失败集合通过数表述为全仓通过，也没有重复运行已知权限条件未改变的全仓覆盖率。

当前已询问用户提供符号链接权限条件，尚未收到答复；不自行修改操作系统安全配置，不跳过权限用例。完整性能基准、D/W 最终逐项证据和其他未完成门禁仍需继续，W11 延期且不接真实登录。本批未提交 Git。
