# 预设会话选择器与历史标识验收

日期：2026-09-23。新会话选择器和已有会话只读标识已接入独立预设插件，设置保存后同步已打开空白会话仍未实现。第二优先整体未验收，未提交 Git。

## 实现

qs-composer 为欢迎区声明 qs.workspace.hero.agentPreset；独立预设插件贡献选择器并订阅官方会话列表，读取共享目录，应用健康条目。会话标题贡献只读名称，缺失预设投影时隐藏，不以全局默认值伪造历史状态。QS 欢迎槽移除时取消会话订阅并清理暂存选择，避免切回官方后应用旧选择。

## 验证

- 相关两包 16 个测试文件、83 个用例通过。预设包覆盖率语句 269/269、分支 186/186、函数 77/77、行 158/158，均 100%；此覆盖率范围不包含 qs-composer。
- 根级 Host/Client 类型检查通过，修改源文件及浏览器测试定向 lint 通过，两个包 tsdown 构建通过。
- 真实浏览器场景 1 通过、16 未选中，8.61 秒；新增空白会话预设选择器快照。历史会话保留标准模式，新建会话采用保存的 ptc 默认值，调用真实 agentPresets.select 切换到 standard 后 Host 回执成功、控件同步。保存、刷新和双界面切换的既有断言同时保留。

首轮组件命名 Seat.tsx 与 seat.ts 在 Windows 编译产物中冲突，改名 PresetSeat.tsx。欢迎区夹具按插槽名称区分两项贡献，生命周期夹具统一从一个根声明子槽。首次浏览器缺少新标签时，确认两个浏览器包未重建；重建后原断言通过，没有弱化历史会话保持原预设的要求。

日志为 logs/repair-v1-preset-seat-ui-*.log。doc-sync 34 项通过、0 失败，55.33 秒。未将本轮定向通过表述为全仓验收。

## 后续

设置保存前必须捕获空白会话标识，保存后仅同步仍为同一空白会话的生效预设。还需验证无会话暂存后首次发送的提交顺序，避免选择 RPC 与发送竞争；其他 W10 与 D/W、安全性能及最终全量验收仍保持未完成。

官方生成目录和依赖锁更新见[75-预设会话插槽登记](../../../官方源码改动记录清单/75-预设会话插槽登记.md)。业务实现与新增插槽均在二开包，不修改官方会话或预设服务。
