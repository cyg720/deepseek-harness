# 全仓覆盖率缺口复核

日期：2026-09-24。本轮补测试，不改变产品行为、不降低逐文件 100% 门槛，也不删除原失败证据。

## 原失败与补充断言

| 文件 | 原门禁结果 | 新增验证 |
| --- | --- | --- |
| `packages/host/directory-picker-auto/src/index.ts` | 分支 91.66%；第 117 行附加呈现缺省分支未覆盖 | 直接调用公开 apply 缺省参数，分别验证 native/browse 仅挂载对应官方两项，卸载后无存留；真实 Loader 和 Fiber，平台选择固定 |
| `packages/test-support/client-runtime/src/assembly/test-client.ts` | 函数 95.45%；observe/disconnect 未覆盖 | 真实 TestClient 启动后观察并释放 DOM 元素，验证 jsdom 替身不伪造布局通知，释放运行器后移除其安装的全局替身 |

测试分别位于包内 `tests/qs/additional-surfaces.spec.ts` 和 `tests/qs/resize-shim.client.spec.ts`，属于二开自有文件，不进入官方原文件登记。新增范围不涉及 README 行为契约或 Agent Note 架构决策变更。

## 已执行

Windows 定向命令 `pnpm exec vitest run packages/host/directory-picker-auto/tests/qs/additional-surfaces.spec.ts packages/test-support/client-runtime/tests/qs/resize-shim.client.spec.ts`：2 文件、8 项通过，退出 0，2.54 秒。日志为 `logs/repair-v1-final-coverage-gaps.log`。

定向 oxlint 首轮发现两个箭头参数括号格式问题；修正后退出 0，日志为 `logs/repair-v1-coverage-gaps-lint-fixed.log`。没有修改断言。

`pnpm run typecheck` 退出 0，日志为 `logs/repair-v1-coverage-gaps-types.log`。Linux 非 root 的 `pnpm run hygiene` 为 16 项通过、0 失败、0 跳过，15.03 秒，日志为 `logs/repair-v1-linux-hygiene-final.log`；其中 Cordis config 与 vendored links 均通过。该结果证明正确符号链接检出下门禁通过，不代表主机 Windows 的普通文件检出已经修复。

`pnpm run doc-sync` 退出 0，日志为 `logs/repair-v1-coverage-gaps-docs.log`。本轮没有提交或推送 Git。

Linux 非 root 容器再次运行 `pnpm run test:coverage --maxWorkers=4`，退出 0，耗时 406.97 秒：1,454 个文件通过、11 个文件跳过；23,522 项通过、1 项预期失败、125 项跳过，共 23,648 项。统计范围内语句、分支、函数、行均为 100%，逐文件门槛通过；日志为 `logs/repair-v1-linux-coverage-complete.log`。覆盖范围沿用仓库已有配置及平台排除，不包含所有进程外、真实在线服务和浏览器场景。容器收到相同测试行为版本；主机随后仅补充箭头参数括号。

## 仍需单独验收

本次测试不代替原生 Ctrl+F、完整原型状态、同页面连接替换、性能基线和安全可达性验收。浏览器控制工具本轮返回 `nodeRepl.fetch request failed`、可用浏览器为空，未执行原生查找；保留待验，不将 beforematch 模拟作为原生查找证据。SDK 的受限 bash 仍受容器 SANDBOX_UNAVAILABLE 影响，未降低隔离策略。

重新执行 `pnpm audit --json` 退出 1：0 严重、2 高危、17 中危、3 低危，与前次计数一致；原始数据为 `logs/repair-v1-security-final-refresh.json`。高危调用点缓解见 [172](172-桌面ZIP安全缓解验证.md)，不得据此将扫描标为通过。静态检查确认 Mermaid 入口在文档站点，MCP 消费者显式使用 SDK client transport；这些事实不足以证明传递依赖中的全部公告不可达，仍需逐公告审查相关参数及运行方式。
