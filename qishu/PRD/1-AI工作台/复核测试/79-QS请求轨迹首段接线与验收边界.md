# QS 请求轨迹首段接线与验收边界

日期：2026-09-23。分支 dev-workbensh，未提交。第二优先尚未完成，W9 仅完成首段独立呈现；W11 按用户决定延期并保留说明入口。不接入真实登录。

## 界面依据

界面、样式和交互以 [workbench-priority.js](../../../prototype/workbench-priority.js) 的 trajectory 场景及 [workbench-priority.css](../../../prototype/workbench-priority.css) 为准。官方源码只用于核对请求投影、接口、状态所有权和生命周期，不能替代原型成为外观标准。本轮对齐阅读标签边框、选中态、间距，请求卡内边距、指标网格、上下文滚动高度和响应式断点。未执行像素截图对照，因此不能声明一比一还原或视觉验收完成。

## 实现范围

新增独立 qs-ui-trajectory，对应官方 ui-trajectory，在 qs.stage.view 注册。读取官方 trajectory target 的请求快照，不复制事件 Definition、投影 builder、分页执行器或服务实例。官方 target 移除时注销贡献，重装后重新注册；会话数据和连接状态通过可释放订阅获取。

已展示请求用途、状态、序号、轮次、步骤、模型及耗时；默认折叠系统指令、工具定义和请求配置，展开后以 React 文本渲染。历史缺窗不填充示例数据。分页按钮遵守加载中和离线状态；失败显示本地化限制提示，不直接暴露 Host 错误细节。显式提示消息、工具结果和媒体轨迹尚未提供。聊天与请求轨迹共享官方草稿，切换仅替换阅读区。

## 测试事实

- Host/Client 类型检查通过；轨迹包及浏览器回归文件 lint 通过。
- 新包 4 个测试通过，覆盖注册、卸载、重装、缺失会话拒绝、上下文展开、离线和分页失败重试。
- QS 覆盖率：110 个测试文件、669 个测试通过。语句 4109/4109、分支 3030/3030、函数 1319/1319、行 3058/3058，均为 100%。这不证明计划中尚未实现的功能已被覆盖。
- 真实浏览器全 QS 回归：7 个文件、28 个测试通过，123.96 秒。最终样式对齐后重新构建轨迹及 composer 包，并复测真实轨迹场景通过。未因纯 CSS 调整重复其他已通过测试。
- 浏览器场景覆盖已录制会话请求、上下文默认不挂载、主动展开、760 像素内容容纳、切回聊天保留未发送草稿；原型示例内容不作为实际请求数据。
- 文档门禁首次发现配置目录过期、双语标记及新包 README 格式缺失。修正后 doc-sync 34 项全部通过，53.59 秒。

日志位于本目录 logs/repair-v1-trajectory-*.log，不提交运行日志。

## 本轮问题及修正

浏览器首次失败发生在缩小视口后：布局按断点恢复侧栏，侧栏覆盖了聊天标签。测试原先在调整视口前关闭侧栏，未覆盖断点行为；改为跨断点后通过可见开关收起，再点击正文。未使用强制点击或改变原型交互绕过问题。

## 尚未完成

W9 消息、工具结果及媒体记录，定位请求消费，完整用量/首字延迟指标，长轨迹性能评估，视图标签完整键盘交互，以及与原型的完整视觉核对仍待补齐。本轮没有完成真实模型 API、全仓库覆盖率、安全依赖公告复查或性能基准，不可据此声明第二优先整体通过。

本轮官方原文件登记见 [第 55 批](../../../官方源码改动记录清单/55-请求轨迹插件接线登记.md)，二开自有文件不纳入官方源码记录。
