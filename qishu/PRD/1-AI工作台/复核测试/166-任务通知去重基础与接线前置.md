# 任务通知去重基础与接线前置

日期：2026-09-23。本批推进 G07，不代表通知界面已经可用，也不代表第二优先整体验收完成。

## 已实现

在 qs-ui-jobs 增加内部 JobNotificationTracker，接受同一次观察的作用域身份、控制流基线、就绪状态、可访问会话范围和官方作业镜像。仅先观察到 running/stopping，再观察到 completed/failed/killed 时产生通知候选；首次终态、重复发布、换作用域、换基线、非就绪恢复和显式清空均静默。以会话、JobId、startedAt 共同区分身份，移除不再可访问或不在镜像中的记录，按调用方提供的容量限制去重集合。不保留结果正文，不更改官方任务状态。

增加 4 项定向测试，分别覆盖终态与去重、重连与退出、会话与尝试身份隔离、容量淘汰。qs-ui-jobs 全包 3 个测试文件、9 项通过，限定该包的四项覆盖率均 100%：Statements 93/93、Branches 73/73、Functions 29/29、Lines 62/62。日志：`logs/repair-v1-job-notification-tests.log`。

正式 `pnpm run typecheck` 通过，日志：`logs/repair-v1-job-notification-types-verified.log`。首轮误从 Host 根入口导入 SessionJob 导致 TS2878，已改为与既有 Jobs 视图一致的 `/types` 入口后重新通过；未使用断言或放宽编译配置。定向 Oxlint 与 `git diff --check` 通过，README 中英文及配对记录已更新。

本批 doc-sync 首轮为 33 项通过、1 项失败，失败为 export-jsdoc 扫描了错误导入所生成的 `packages/api/session-controller/src/*.d.ts`。逐项核实 48 个未跟踪 JS、声明及 map 文件创建时间均为本轮失败编译的 22:18:29–30，且有对应 TS 源文件后，仅移除这些生成副本。随后 `pnpm run verify-export-jsdoc` 单项复跑通过；未修改官方源码、关闭检查或为编译副本补注释。日志保留于 `logs/repair-v1-job-notification-docs.log`，不将首轮失败改写为一次性全绿。

## 接线仍未完成

当前跟踪器尚未由插件生命周期装配，toast 不会自动显示作业转换。下一步必须把配置、当前已打开且可访问会话、Host/退出作用域与 toast 消费接通，并执行真实 Host 和浏览器 T39，不能以这 9 项测试替代业务通知验收。

源码确认 SessionControlOwner 在 accept(frame) 返回后同步发布控制基线，SessionManager 的领域列表经 Notifier 微任务批处理才由 Sessions.projectList 发布。因此不能在 control.state 的 ready 通知里立即读取旧 jobsBySession 并将后续列表变化当作新终态。消费者需明确协调发布批次并通过可控时序验证；此时序说明是接线约束，不表示官方控制流本身有错误。

容量需要纳入经验证的部署配置，不能将本测试选择的容量硬编码成产品默认值。作用域身份须来自实际 Host/连接所有者，不能用会话标题代替。初始基线、重连基线、切会话、换 Host、退出、卸载及迟到回调均为后续接线的必测项。

本批只改 QS 自有文件，不登记到官方原文件清单；未提交或推送 Git。W11 继续延期，不接真实登录。
