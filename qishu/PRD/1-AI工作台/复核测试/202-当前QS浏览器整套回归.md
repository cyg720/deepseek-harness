# 当前 QS 浏览器整套回归

日期：2026-09-24。Windows 本地工作区，分支 dev-workbensh。对当前已有 QS 浏览器场景进行一次完整组合执行，验证此前分别新增的分页重连、设置冲突、命令目录故障及回答交付场景能与其余场景共同运行。

执行命令：设置 DSH_SNAPSHOT=replay 后运行 `node node_modules/vitest/vitest.mjs run --config vitest.web.config.ts apps/web/tests/qs`。未筛选测试名称，未刷新快照，未降低断言或修改配置。日志为 `logs/repair-v1-current-web-all.log`。

结果：10 个测试文件、72 项测试全部通过，退出码 0；16:36:50 开始，用时 329.81 秒。日志包含真实 QS 插件整族两轮重载记录。本次替代报告197的64项作为当前已有 QS 浏览器用例的整套执行证据。

范围限制：这是已有 QS 浏览器用例的全量执行，不是全部计划业务组合、全仓测试或性能门禁通过证明。未实际驱动操作系统原生目录弹窗。仍需补齐验收总表中的远端 memory 设置、原型状态与性能等缺口；不依据本次通过自动关闭 T01–T48 的其他部分项。

远端设置的测试入口：官方 `apps/web/tests/remote-welcome.e2e.ts` 使用 `launchWebScaffold({ remoteAuthority: 'remote.localhost' })` 进入非回环访问；客户端设置按连接的 isLoopback 事实选择 host 或 memory。以下增量复用该夹具，不把组件模拟状态当成真实部署证据。

## 远端设置增量

整套执行后，已在 QS model-settings 场景中新增真实非回环域名验证。使用现有工作台配置，完成远端仅浏览器有效的欢迎确认，打开设置并核对无持久设置的提示、无本机配置文件入口；将忙碌 Enter 偏好切换为 steer 后读回控件状态，打开插件配置并核对不可持久化提示。全过程只观察到预设目录能力的只读查询，没有设置 describe、更新或本机文件打开调用；实际 Host settings.yaml 内容保持不变。没有模拟 settingsScope，也没有拦截或拒绝网络请求。

Windows 定向结果：1项通过、3项未选中，6.46秒，退出码0，日志 logs/repair-v1-remote-settings-final.log。最终文件 lint 通过；Host 类型检查在该新增场景首次修订后通过。早期失败日志保留：首次缺少工作台配置，第二次未完成远端欢迎确认，第三次错误地把合法只读能力查询也列为禁止请求；最终断言明确列举唯一允许请求，不笼统放行 settings 路径。该增量不并入前述72项的同次执行，也不代表所有设置字段和远端模型凭据动作已完成验收。

刷新边界：同一真实远端页面刷新并重新完成静态登录和欢迎确认后，busyEnter 从 steer 恢复为默认 queue；Host 文件不变，仍只有只读目录能力查询。随后执行整个 model-settings 文件，Windows 4项全部通过、无筛选，26.93秒，日志 logs/repair-v1-remote-model-all.log。最终源码的 Host 类型检查、文件 lint 均退出0，日志 logs/repair-v1-remote-refresh-types.log 和 logs/repair-v1-remote-refresh-lint.log。

Linux 容器同步该测试文件后，整个 model-settings 文件4项全部通过，24.73秒，日志 logs/repair-v1-remote-model-linux.log。文档快速检查16项通过、无失败或跳过，17.37秒，日志 logs/repair-v1-202-docs.log。未修改生产源码、官方原文件或任何覆盖率门槛。

## 设置截图取景补充

原 repair-v1-model-settings-dialog.png 在长供应商列表底部取景，未包含标题与分区导航，不能据此认定导航缺失或完成整体视觉验收。现保留该截图，并新增滚动至顶部的 repair-v1-model-settings-top.png。实际查看新图可见 Settings 标题、关闭入口、分区导航、DeepSeek 表单和空密钥输入框；本样本未见横向文字溢出。截图位于本报告目录 logs 下，使用合成端点，不包含真实密钥。

新增截图的真实配置场景1项通过、3项未选中，12.28秒，日志 logs/repair-v1-settings-visual.log；最终文件 lint 通过。该图仅补齐此样本的顶部取景，尚不能证明所有设置分区、语言、主题与原型状态一致；本轮没有根据截图取景差异改动生产样式。
