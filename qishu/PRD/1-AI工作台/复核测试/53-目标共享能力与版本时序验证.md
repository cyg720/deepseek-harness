# 目标共享能力与版本时序验证

日期：2026-09-23。W7/C15/T26/T45 的接口准备，尚不是目标界面完成。

## 已实现

官方 ui-goal 提供 goalPresentation.bind(sessionId)，官方 dock 与后续 QS 呈现复用同一实现。每个视图的激活源仍只在有观察者时读取/订阅；没有新增目标执行器或重复注册 command-input Definition。类型通过官方 client 公共入口导出，自有声明放在包内 qs 子目录。

四个动作增加可选 expectedRef：省略时保持官方按当前投影取 CAS ref 的行为；传入时保留用户看到的 id/revision，交给现有 Host CAS 拒绝过期修改，禁止自动升级旧草稿的版本。连接重置清除旧进程的 activation，等待新查询或事件，不把旧 armed 沿用到新连接。

## 验证

目标包 5 文件、43 用例通过；语句 210/210、分支 130/130、函数 54/54、行 177/177，逐文件四项 100%。日志 logs/repair-v1-goal-presentation-coverage.log。

可控连续重连验证旧读不能覆盖新代次。新增真实 ProjectionValueStore 测试覆盖值先可读、微任务通知后到的空目标响应路径；去掉原本对此路径的覆盖率排除，以实际行为证明两分支。共享 API 测试验证动作载荷、调用时投影、显式旧 ref、不重复历史定义、依赖卸载移除提供者。编译、官方浏览器和工程检查结果见下。

## QS 目标界面仍须实现

独立 qs-ui-goal 的 dock 与 command-input 行、合法创建入口、目标加载/空态、active 激活读取/armed/disarmed、paused、blocked 的 code/message、complete 正文和轮数都未交付。轮数显示 roundsStarted/maxGoalRounds，不画完成百分比。编辑失败保留草稿、明确版本冲突与刷新/重新编辑、请求拒绝后释放锁、切换会话/目标时迟到结果隔离、卸载和真实 Host 往返仍待实现与验证。不能直接复制官方 GoalBar 的“完成即隐藏”行为，因为计划要求保留完成正文。

W11 延期，仍不接真实登录；没有提交或推送 Git。官方原文件见第 37 批登记，二开自有文件不列入官方清单。

## 本阶段最终检查

- 目标 Host：3 文件、47 测试通过，日志 logs/repair-v1-goal-host-tests.log。
- Client 编译通过；官方目标 bundle 构建通过，日志 logs/repair-v1-goal-presentation-types.log、logs/repair-v1-goal-presentation-bundle.log。完整 lint（含 Host 构建）最终通过，日志 logs/repair-v1-goal-presentation-lint-final.log。
- 完整 doc-sync 最终 34 通过、0 失败、0 跳过，57.14 秒。新增 Client 服务已登记到其 README 文档 owner；不是关闭全局扫描。日志 logs/repair-v1-goal-presentation-doc-sync-final.log。目录单项检查确认 97 个生成区域为最新。
- 官方目标浏览器扩大检查：3 文件中 2 通过、1 失败；5 个测试通过、1 失败、1 跳过（仅用于真实模型重新录制的模式分支）。日志 logs/repair-v1-goal-presentation-browser.log，不能宣称目标浏览器全部通过。

## 新发现的 Windows 回放问题

失败来自 goal-multi-turn-actions.e2e.ts：录制及预期使用 bash 工具，但 standard/agent.cordis.yml 在 win32 上禁用 tool-bash 并启用 tool-pwsh；实际出现 unknown tool "bash"，造成工具转写与持久化回放快照不一致。两轮仍结束、目标条及目标指令历史的其他场景通过，但不能用这些替代失败场景。没有更新 golden、删除断言或新增跳过，平台回放装配还需修复或取得兼容运行环境的真实验证。

本轮不改变记录过的 Session 代次，也不启用真实模型重新录制。此前 ACP 符号链接占位和文件符号链接 EPERM 仍保留为未解决项。完整第二优先、QS 目标界面及全仓验收仍未完成。

后续平台核查见 [54-目标回放跨平台核查](54-目标回放跨平台核查.md)：当前源码在隔离 Linux 下三份目标浏览器文件全部通过、媒体引用 22 用例通过、hygiene 16 项通过；Windows 原失败结果保留。另确认 GoalError 的领域 code 不被 Gateway 作为类型化 RemoteError 保留，QS 版本冲突呈现仍须完成真实载荷验证及处理。
