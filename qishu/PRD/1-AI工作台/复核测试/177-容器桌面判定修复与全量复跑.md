# 容器桌面判定修复与全量复跑

日期：2026-09-24。容器与真实 WSL 桌面分派已修复，整体第二优先验收仍在进行。

新增四项容器回归；修复前两项失败、两项通过。修复复用现有依赖树的 is-inside-container，并给测试事实增加 insideContainer；显式 WSL 用例排除测试运行器本身的容器影响。真实默认宿主用例验证管理器标识与最终命令一致，不再假定 Linux 一定不是 WSL。没有通过跳过测试消除失败。

Windows 与 Linux 非 root 环境定向均为 4 文件、83 项通过，日志为 `logs/repair-v1-container-path-windows.log`、`logs/repair-v1-container-path-linux.log`；负向证据为 `logs/repair-v1-container-path-negative.log`。

新增依赖仅声明已有的 is-inside-container 1.0.0，不升级版本。包管理器附带的无关 peer 解析重排已恢复；frozen-lockfile 离线安装验证通过。README 双语及配对记录、Agent Note 和官方第 101 批记录同步更新。全仓类型、文档与覆盖率复跑结果待补。

全仓 typecheck 退出 0；定向 oxlint 退出 0；构建产物的容器/真实 WSL 四项分派断言通过。doc-sync 为 34 通过、0 失败、0 跳过，121.80 秒。SDK 受限 bash 以非 root 复跑仍返回 SANDBOX_UNAVAILABLE，没有降低隔离策略。

新增直接运行时依赖要求更新 THIRD_PARTY_NOTICES.md。全量复跑检出遗漏后已执行原生成器，补入 is-inside-container 的 MIT 声明；专项 30 项通过，日志 `logs/repair-v1-container-notices-fixed.log`。该生成文件已追加第 101 批官方记录，当前总表 195 个官方原文件，193 项保留、2 项已还原。全量复跑终态及修复后覆盖率仍待记录，不能将专项成功当作全量门禁成功。

该次全量终态：1,453 个文件通过、11 个跳过；23,519 项通过、1 项预期失败、125 项跳过，耗时 390.40 秒。覆盖率仍失败：directory-picker-auto 的缺省附加呈现分支，以及 client-runtime 的 ResizeObserver 替身两个方法未覆盖。日志为 `logs/repair-v1-linux-coverage-final.log`；后续补测和终态见 [178 覆盖率缺口复核](178-全仓覆盖率缺口复核.md)。普通用例全部通过不代表该命令成功。
