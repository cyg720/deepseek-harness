# 图片预览阶段验收

图片子插件独立对应官方 image 正文，消费文档宿主完整字节，保留八类 MIME、原始尺寸、异步解码和失败提示。SVG 只在 img 静态图片上下文中显示；替换和卸载释放 Blob URL，旧图事件不能改变新图。

## 已执行

- 5 个测试文件、20 条测试通过，包括八类格式、解码失败、对象 URL 创建失败、替换和释放、错误内容及旧图事件隔离。
- Client 类型、定向 oxlint 与预览包 tsdown 构建通过。
- 真实浏览器定向场景 1 条通过、2 条因筛选未运行。真实 Host 从隔离工作区读取带 script/onload 的 SVG，图片正常解码为 120×80，应用 DOM 没有 SVG/script/iframe/object，父页面标记未被脚本设置；同场景继续验证文本、Markdown、代码切换及右栏几何。
- 新包覆盖率仍未通过：语句 94.08%、分支 86.04%、函数 94.23%、行 95.86%；图片新文件达到逐文件四项 100%，Preview.tsx、index.ts、text/index.tsx 仍不达标。

## 未完成

HTML、PDF 正文；预览宿主剩余分支覆盖、完整安全性能及全量验收。图片测试通过不代表已对任意格式做安全认证，SVG 结论限于实际验证的静态 img 使用方式。

证据见 logs/repair-v1-qs-preview-image-*，官方生成目录登记为第 26 批。
