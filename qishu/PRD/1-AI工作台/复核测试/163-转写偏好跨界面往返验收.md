# 转写偏好跨界面往返验收

日期：2026-09-23。范围：同一 Host 的官方 Chat 与奇术转写共享偏好。

## 真实浏览器证据

在既有模型设置集成场景中增加转写偏好断言：奇术先保存 normal 并刷新确认；首次切到官方通用设置必须显示 Normal，选择 Compact 后 settings.yaml 出现 transcriptView: compact，返回奇术选择框必须为 compact；第二次切到官方必须显示 Compact，改为 Normal 后再次核对落盘和奇术回显。没有在两个呈现端各建存储，也没有在测试中直接改浏览器偏好缓存。

先运行首个定向场景通过，再执行 model-settings.e2e.ts 整个文件：3 项全部通过，包含已有自定义提供商操作和实际适配器模型发现。没有发送真实模型请求。定向 lint 通过；正式 pnpm run typecheck 通过。日志为 repair-v1-transcript-cross-ui.log、repair-v1-transcript-cross-ui-suite.log、repair-v1-transcript-cross-ui-types.log。

## 后续差异

源码核查确认官方通过 useSearchableHidden 保持隐藏子树挂载、使用 hidden=until-found 并响应 beforematch。奇术当前过程成员在折叠时返回 null，最终推理使用普通 hidden；这些尚不能证明浏览器原生查找可以揭示折叠内容。后续需要复用官方机制并验证焦点保护和查找揭示，不把本次偏好往返当成查找验收。

最近全体 QS 实测仍为报告 162 的 159 文件、909 项测试与四项逐文件 100%；本批仅新增浏览器断言，不重新声称已运行全体 QS。全仓、安全性能与整个 D/W 验收仍未完成。未提交或推送 Git，不接入真实登录，W11 保持延期。
