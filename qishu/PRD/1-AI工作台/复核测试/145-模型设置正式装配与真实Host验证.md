# 模型设置正式装配与真实 Host 验证

日期：2026-09-23。正式 Web bundle 已加入独立 qs-ui-settings-models，官方模型目录和欢迎确认控制器保持唯一。此阶段不代表第二优先或 W10 全面完成。

## 实现与构建修正

Web 配置和 resolver manifest 同时增加插件，更新 workspace 链接、锁文件、配置目录及客户端目录。新增浏览器用例位于 apps/web/tests/qs/model-settings.e2e.ts，以真实官方 Web scaffold、隔离 Harness home 和真实 Chromium 执行，不接真实登录或外部模型请求。

初次完整构建暴露新用例误入 Client 编译程序，导致 Host Context 和源文件编译范围错误。已按照既有浏览器用例规则从 apps/web Client 配置排除，并纳入根 Host 配置。修正后 pnpm run build 完整通过，记录 302 个 Client 构建产物。没有修改 vendor 或放宽类型检查。初次失败构建曾在源码旁生成 496 个未跟踪的 JS、声明及 map 文件；按同名源码和失败构建时间窗口逐项核实后清理，删除路径经过仓库范围校验，清单保留在忽略提交的 repair-v1-build-residue.json。没有删除手写或已跟踪文件。

## 真实浏览器证据

- 欢迎说明与 DeepSeek 凭据步骤按顺序呈现，两份 ARIA 快照已生成；普通快照模式再次执行通过。
- 点击明确确认后，实际 settings.yaml 保存当前欢迎版本；密钥实际写入隔离 .credentials.yaml。
- 密钥输入不回显已保存值，页面 HTML 和浏览器控制台不包含测试密钥。测试使用随机合成密钥，不调用外部服务。
- 模型设置读取到已配置凭据，编辑端点后实际持久化。页面刷新及重新进入模拟登录后，端点仍为保存值、密钥字段为空，已完成引导不再保留在页面中。
- 全程 prompt 请求数为 0，页面运行错误为 0。
- 真实页面的引导和设置截图已人工检查，没有观察到水平溢出；设置内容超长时在对话框内滚动。截图与命令日志位于忽略提交的 logs/repair-v1-model-settings-*。
- 定向 lint 通过。全量构建通过。模型设置包局部证据见第 144 份报告；本轮还重新执行整个 packages/qs 的测试与逐文件覆盖率，157 个测试文件、883 个用例全部通过，语句 6452/6452、分支 4856/4856、函数 2002/2002、行 4518/4518，四项均为 100%。QS 回归不能代替全仓和完整 D/W 浏览器验收。

- 清理失败构建残留并重新生成目录后，完整 doc-sync 34 项通过、0 失败、0 跳过，用时 51.13 秒。

## 后续验收范围

仍需验证自定义供应商增删改、模型发现与编辑、权限或网络失败、跨界面切换和窄屏等浏览器场景。启动时是否出现短暂引导闪烁尚未测量，不以刷新后的最终 DOM 断言替代。原型设置分区、通知、诊断、长历史与恢复、安全性能及全量 D/W 验收继续保留原目标。

本批官方原文件变动已记入第 84 批及总览，二开自有文件未登记为官方源码。未提交 Git。
