# 最终答案推理过程联动验证

日期：2026-09-23。范围：最终答案推理跟随同轮过程显隐，界面保持奇术折叠样式。

## 实现

对照官方 AssistantNodeView 与 ChatNodeSeat：只有紧凑模式、可折叠的已完成轮、最终答案步骤及 inlineReasoning 同时成立时，收起过程才隐藏答案的推理入口。普通模式、运行中、历史不完整、非最终步骤或非内联推理不受该隐藏逻辑影响。答案正文始终呈现；推理 details 保留挂载和自身展开状态，不在显隐间销毁内容。

转写宿主向行传递 reasoningHidden，助手行只据此控制推理入口。生成目录同步新增业主属性，官方原文件变更见 91-最终答案推理行属性登记.md；没有改动官方投影或持久化协议。

## 验证

转写包 11 个文件、116 项测试通过；四项覆盖率均 100%：Statements 554/554、Branches 487/487、Functions 178/178、Lines 421/421。加强运行中和历史不完整夹具，使它们实际含内联推理，再运行过程文件 18 项回归通过。

新增真实浏览器场景在官方 Session 生成的持久日志中加入推理块，保留消息身份及事件顺序，经真实 Host 和 Chat 投影进入奇术转写。验证紧凑时推理隐藏、过程展开后恢复、推理可独立展开、再次收起后隐藏、普通模式恢复入口，以及答案始终只有一份；页面运行错误为空。不向真实模型发送请求。命令只运行该场景，其余 20 项被筛选，不宣称整个交互文件全通过。

包构建及定向 lint 通过。浏览器夹具最初错误地把物理文件头当作 SessionHeader 领域对象，已改为原样保留首行并对后续 SessionEvent 做有类型处理；正式类型检查与重跑后的 34 项文档检查通过。首次文档检查发现生成目录过期，已通过既有生成器更新。日志前缀 logs/repair-v1-answer-reasoning-。

## 全体 QS 回归

进一步运行 packages/qs 全体测试和源码覆盖率，首次出现 6 个组合夹具失败：旧 qs-shell 测试未提供现在必需的 conversationPresentation 与 chatPresentation。夹具补入官方 createConversationStore 和实际可订阅的偏好源，动作若在仅注册阶段意外执行则直接抛错；同时保证挂载提前失败也由 afterEach 释放运行时，没有移除依赖检查或跳过用例。

重跑 159 个文件、909 项测试全部通过。全体 QS 四项覆盖率均 100%：Statements 6510/6510、Branches 4927/4927、Functions 2026/2026、Lines 4559/4559。对应日志为 repair-v1-transcript-all-qs-coverage-final.log。夹具显式依赖的锁文件变化登记于 92-组合测试共享呈现依赖登记.md。补齐依赖后正式类型检查和 34 项文档门禁再次通过，生成目录所属 cordis-client-runner 包也完成构建。此结果仍不是全仓测试或业务范围全部交付证明。

## 剩余范围

原生查找定位、跨官方界面的偏好往返、完整 D/W 功能及安全性能与最终全量验收仍须继续。此批不能标记第二优先整体完成。W11 延期，保留说明入口，不接真实登录；未提交或推送 Git。
