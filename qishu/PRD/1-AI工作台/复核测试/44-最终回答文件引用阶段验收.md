# 最终回答文件引用阶段验收

日期：2026-09-23。范围为 C9/W1 助手最终回答内的文件引用，不代表 C9 或第二优先整体完成。

## 行为与官方对应

QS AssistantRow 对照官方 AssistantNodeView：只有已关闭轮次，且 turn-tail 的最终回答序号与当前回答一致，才启用官方 chatFileMentions。流式内容、中间回答和未知文件保持普通文本；解析和歧义判断沿用官方提供者。引用点击经 fileAddressFor 与 sidebarRight.openResource，使用当前查看会话的工作区。未新增 Host 读取接口或本机打开动作。

转写行新增轮次位置 hook，直接订阅官方 ConversationLocationDataStore；迟到的收尾信息可触发更新，无需扫描全部消息。卸载释放订阅，未复制轮次状态。

## 已执行验证

- 全部 QS 单元及覆盖率命令：`pnpm run test -- packages/qs --coverage --coverage.include='packages/qs/*/src/**/*.ts' --coverage.include='packages/qs/*/src/**/*.tsx'`；84 文件、463 测试通过，语句 3039/3039、分支 2222/2222、函数 971/971、行 2316/2316，逐文件四项 100%。日志 logs/repair-v1-mentions-all-qs.log。
- 新回归验证迟到收尾、最终回答序号替换、非最终回答、未关闭轮次、缺少位置、卸载及正确会话路径载荷。
- 定向真实浏览器场景通过：从实际持久会话的最终回答点击 report.txt，侧栏显示 Host 读取内容；未发出 /api/present.open 请求。该过滤命令 1 项通过、3 项未选中，不能称为全部浏览器通过。日志 logs/repair-v1-mentions-browser.log。
- `pnpm exec tsc -b tsconfig.client.json` 通过；受影响 QS 转写、组合 fixture、工具 fixture 和浏览器测试定向 lint 通过；转写 bundle 已通过。

## 验收边界

附件输入、消息、工具、轨迹四入口仍待实现，C9 不关闭。全仓回归、安全与性能总验收尚未完成。W11 延期，只保留说明入口，不接真实登录。

## 补充门禁结果

全部 QS 浏览器回归：5 文件、15 场景全部通过，71.39 秒，无跳过、无快照更新参数；日志 logs/repair-v1-mentions-all-browser.log。test:docs：16 项通过，16.34 秒；槽目录一致性与定向 git diff --check 通过。

完整 doc-sync：30 项通过、4 项失败、0 跳过（65.93 秒），不能声明所有仓库门禁通过。日志 logs/repair-v1-mentions-doc-sync.log。待修复：

1. verify-cordis-catalog：deliverablesPresentation、documentPreviewPresentation、documentPdfPresentation、sidebarFilesPresentation 四个共享服务未纳入服务文档映射或有文档责任人的明确豁免。
2. verify-cordis-inspect-catalog：公开类型传递到 PresentedOpenController.state，该成员缺少显式类型注解，Typert 分析失败。
3. verify-config-catalog：docs/config-catalog.md 生成结果过期。
4. verify-tsconfig-paths：tsconfig.base.json 与生成器结果不一致。

这些是当前工作区共享接口与目录集成的验收缺口，需修复后重新跑失败门禁；不通过修改阈值、屏蔽检查或把失败归类为环境问题来绕过。

附件范围以 [46 范围校正](46-附件共享接口阶段验收.md) 为准：新增上传明确排除；本文此前列出的附件输入待办不再作为本期交付要求，已有附件查看仍需完成。
