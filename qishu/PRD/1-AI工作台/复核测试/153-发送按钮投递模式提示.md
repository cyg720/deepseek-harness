# 发送按钮投递模式提示

日期：2026-09-23。范围：发送按钮的可访问名称、鼠标提示及实时模式订阅。

## 实现与验证

奇术输入槽新增 qsSubmitMode 可观察源，其 getSnapshot 与实际发送动作共同调用官方共享解析器。源订阅该会话和官方忙碌 Enter 偏好，释放时解除两端监听。忙碌时按钮显示“加入队列”或“引导当前轮”，空闲时为普通发送，创建中仍显示正在发送。中英文文案均归本包词典。

DOM 回归在同一个组件中改变共享模式和运行状态，验证按钮名称与 title 一致。注册工厂回归验证无会话、目标会话和缺失会话的解析参数，验证两端通知和解除监听。输入包及共享 Host 入口 12 个文件、80 项通过；语句 530/530、分支 356/356、函数 173/173、行 383/383，四项均为 100%。

正式类型检查、输入包构建、定向 lint 和翻译配对通过。首次 lint 的不必要类型断言已移除。workbench-flow.e2e.ts 的发送到持久转写集成流程通过；它使用模拟 DOM，不等同于真实浏览器的忙碌投递验收。该运行虽然 3 项断言通过，日志仍出现 dialog.current.showModal is not a function：模拟 DOM 的 dialog 支持未补齐，不能作为无运行时异常的验收证据，需修复测试环境并重跑。

日志前缀：logs/repair-v1-submit-label-，包括 coverage、types、build、lint、pairing、flow 和 docs。文档门禁首次因英文概述 110 词超过 100 词失败，行为说明已移至使用章节，未放宽上限。修订后完整 doc-sync 34 项通过、0 失败、0 跳过，用时 51.69 秒。

## 后续

空草稿加速发送、命令菜单与修饰键组合、真实忙碌会话 queue/steer 投递结果仍需继续验证或补齐。设置行保存、刷新、跨界面和窄屏已由 152 报告验证，不能替代真实消息投递结果。其余 D/W、安全、性能及最终全量验收保持原范围，W11 延期，不接入真实登录。仅修改 QS 自有文件，未提交 Git。
