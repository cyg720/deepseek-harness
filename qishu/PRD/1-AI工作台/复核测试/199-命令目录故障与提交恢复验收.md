# 命令目录故障与提交恢复验收

2026-09-24，dev-workbensh。对应 T12，新增 QS 自有浏览器测试，没有修改官方或 QS 生产实现，未提交 Git。

测试位于 apps/web/tests/qs/transcript-follow.e2e.ts，名称 retains a slash draft when the command directory fails and executes it after recovery。使用真实 Web 装配与官方录制历史，仅中断 /api/commands/list；输入 /permission read-only 并提交后等待页面错误，确认目录请求确实失败、草稿原样保留、没有 /api/session/prompt 请求。解除目录故障后再次提交，真实命令卡显示 Command completed，草稿清空，普通消息请求仍为零。

首次测试在打开历史后直接查活动 Session，因历史读取不保证 Host 已挂载活动 Agent 而失败。已修正测试假设，使用实际网络请求观察是否误发普通消息；保留首次失败日志 repair-v1-command-directory-failure.log，不将其归为产品缺陷。

Windows 最终定向用例通过（repair-v1-command-directory-failure-fixed.log），Linux 相同用例通过（repair-v1-command-directory-linux.log）。Host 类型检查及该文件 lint 均退出码0（repair-v1-command-directory-types.log、repair-v1-command-directory-lint.log）。其他13项未选中，不计入本次通过数。

本测试覆盖目录读取失败后的提交保护和再次提交恢复，不证明每个命令的副作用、所有输入来源及热卸载竞争。菜单候选读取失败的外观仍沿用官方控制器行为；此次没有增加另一套命令目录、重试协议或普通发送绕行。
