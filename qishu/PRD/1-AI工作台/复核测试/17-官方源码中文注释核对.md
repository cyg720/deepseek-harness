# 官方源码中文注释核对

2026-09-20。仅补充官方原文件二开位置的中文维护说明；范围及例外见[逐项登记](../../../官方源码改动记录清单/09-官方改动中文注释登记.md)。

- TypeScript：19 个文件；用 TypeScript 解析器分别读取本轮修改前后内容，以 removeComments 打印后逐文件比较一致，并检查新增语法诊断，证明本次源码仅改变注释。
- YAML：Web 插件装配和安全 override 只添加注释；解析对象前后一致。
- Markdown：10 个正文文件添加 HTML 中文注释；对应双语记录重新生成，不影响页面展示。
- JSON、锁文件、自动生成槽位及目录不手工添加注释，逐项登记原因；已还原的检查脚本保持原样。
- 配置目录重新生成并同步中文中的回放源码行号，除此没有扩大原有官方文件修改范围。

## 检查结果

- 限定本轮 19 个 TS/TSX 文件执行 `scripts/run-oxlint.ts`：通过。
- `pnpm run doc-sync`：33 项通过，1 项根 AGENTS.md 字数失败；删除重复注释、保留其既有中文规则后，`scripts/verify-doc-budgets.ts` 单独重跑通过。没有声称重新跑过整组。
- 19 个 TypeScript 文件去注释打印结果一致；2 个 YAML 文件解析结果一致；10 个 Markdown 正文去掉新增 HTML 注释后与本轮修改前一致。
- 初次用默认 YAML schema 解析 Web bundle，因官方 `!!js` 标签失败；改为仅将该标签读取成文本对象后比较通过，没有执行表达式。
- 总表及注释登记的文件链接、锚点校验通过；官方已修改文件清单无漏登。
- `git diff --check`：通过。仅有此前原型文件 CRLF 转 LF 提示。
- 本轮不改变运行行为，未重复执行产品功能或浏览器测试；未提交或推送。

输出日志位于同目录 `17-验证日志/`。

根 AGENTS.md 的改动本来就是中文隔离及登记规则，保留现有说明；未重复添加注释，也未提高文档字数上限。
