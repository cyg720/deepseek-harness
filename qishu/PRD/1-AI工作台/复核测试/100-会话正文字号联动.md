# 会话正文字号联动

日期：2026-09-23。局部样式修复，未提交；第二优先整体验收尚未完成。

## 问题与实现

真实 Host 已保存 17px 正文字号时，QS 历史用户消息仍固定为 13px。新增浏览器回归在旧实现上复现 expected 16 / received 13，证实设置值与显示脱节。用户正文、追加说明及助手容器改为消费官方 --dsh-content-font-size，保留各自相对 14px 默认值的原型差值；助手 Markdown 的内层排版继续使用官方令牌，代码块不随正文字号放大。

## 已执行验证

- 使用官方 Session 生成的 11 轮真实历史装入测试 Host；通过设置控件提交字号 17，并等待官方成功回执。
- 重新构建 QS 转写包后，浏览器设置场景通过：历史用户正文和助手 Markdown 正文均比默认增加 3px；代码块实际计算字号保持不变。设置持久化、草稿、焦点与语言回归继续通过。
- 测试文件 lint 及 README 双语配对检查通过。本轮仅修改 CSS 与现有浏览器用例，不新增接口或官方业务代码。
- 日志：logs/repair-v1-message-font-red.log、green.log、build.log、lint.log、pairing-check.log。

## 验收范围

全部 QS 浏览器回归 7 个文件、30 个场景通过，耗时 134.29 秒，日志为 logs/repair-v1-message-font-browser-all.log；这不等于全官方仓库测试或第二优先需求全部完成。追加说明沿用同一变量计算，但本轮设置场景没有种入追加说明记录，不宣称已单独验证该分支。长历史字号变化时保持同一阅读行的专门断言仍需补齐。

仅变更 packages/qs、apps/web/tests/qs 下的二开文件和本报告，因此不向官方源码改动清单登记。
