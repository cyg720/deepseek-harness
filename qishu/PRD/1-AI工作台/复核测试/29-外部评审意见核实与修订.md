# 外部评审意见核实与修订

日期：2026-09-20。基线：本地 HEAD 906b6de1e2，结合当前未提交的 00–11 计划文档。来稿按旧 10 份文档审阅，不能把其行号或通过数字直接用于当前 12 份计划。此次逐项核对 H1–H6、M1–M7、L1–L6 共 19 项；只修改文档，不改生产/原型程序、不接真实登录、不提交 Git。

## 裁定与处理

| 意见 | 核实结论 | 处理 |
|---|---|---|
| H1 仅斜杠能力隔离 | 现有服务/来源/guard 无消费者级过滤，成立；“一定弹出官方 DOM 菜单”过度推断，状态生成和视图渲染不同。旧 G05 已留接缝备选，并非无路可走 | 09/G05 明确选择新增最小公开接入，不再重复验证现有过滤开关；保留唯一服务/controller，不采第二份控制器或默许 @ 扩围；T37/预算同步。 |
| H2 popupFor 与焦点 | 类型细节成立：Context 合并到具体 CommandUiRuntime，popupFor 返回有类型 controller，窄契约 unknown 不阻止实际消费；bindComposerFocus 仅找到定义及测试调用 | 02 区分实际类型与窄契约，默认不依赖 binder；QS DOM owner 恢复焦点。无生产调用不等于方法不可用，不据此修改官方 API。 |
| H3 右栏子槽 | 三个嵌套槽的 owner 漏列属实；“必须接管官方槽”只适用于原样复用官方视图，不适用于本计划重写呈现 | 02 补槽表，09/G01 明确 QS 自有正文/标题/文档槽，复用官方数据/类型/状态；不采重声明官方已占有子槽的建议。 |
| H4 轨迹视图 | 官方 conversation.view 被遮蔽后不能直接显现属实；C17 原已要求重写，缺标签/记忆与成本明细 | 03/C17、04、09/G03 补 QS 标签、选择/记忆和完整呈现，数据复用不抵扣视图工作；G09 pending 保留。 |
| H5 ui-workspace 依赖/提供 | 源码事实成立；旧表列入口并未明确写“提供 sessions”，但确实容易误读 | 02 明写 inject sessions/workspaces/directoryPicker、自提供 uiWorkspace；sessions 由 api-session-controller 提供，QS 禁止重复提供。 |
| H6 默认 Web 无 picker 客户端 | 结论不成立。只看静态 YAML 漏掉 auto 的动态 Loader 装配，同一基线已有双面装载 | 保留两个条件性 QS 对应呈现；补 G10 动态树核查，不采“browse 仅测试”“native 仅桌面”或删除候选包的建议。 |
| M1 命令冻结 | 新增长驻交互未明确接入既有冻结，成立；但多个插件直接写同一个 reason 会互相解除 | G02/C7/04/05 规定 Composer 唯一汇总 owner，复用 setLocalFreeze；加双原因、迟到回包、取消/失败/卸载释放断言。 |
| M2 队列签名/快照 | 参数名过泛、快照归属不明，成立 | 02 使用 updateQueue(itemId: MessageId, action: QueueAction)，明确 SessionSnapshot 与 SessionFace 订阅，未发明 mutation 接口。 |
| M3 DoD 错计/说明遗漏 | 错计不成立：表中第二优先 8 行、第三优先 6 行，“未知节点兜底”独立；commands/input-trigger 的修订说明已存在 | 保留 14/14，仅加计数说明，不删条款、不调整来源计数。 |
| M4 工期无锚点 | 现有区间缺实际工时校准属实；所给 LoC 不能直接推导工期，呈现族总量混有服务/测试且无完整纳入清单 | 00 明确早期未校准区间不可作承诺，按包×子注册/组件区分复用、重写、公开接入、测试/视觉重估；不杜撰第一阶段工时或采用 LoC 比例换算。 |
| M5 W11 被计入总数 | 不成立。10–18 人日标为 W10，22+18+15+10+8=73，37+30+25+18+12=122 | 不改正确加总；明确 W11 无估算，与未计的接口改造成本分开。 |
| M6 调度写面 | 无现成客户端管理 Remote、无 pause/edit 语义成立；create/list/delete 模型工具不是客户端管理接口 | 02/P02 明确只读基线，额外管理是产品增量；另补源码确认的默认 ui-schedule disabled:true，验收须完整启用 Host/Client。仅要 create/delete 不必顺带强制新增 pause/edit。 |
| M7 settings 模式 | settingsScope 的 memory 持久化不发请求成立，schema/describe 归属需明确；“全部远端设置能力都不可能”过宽 | 02、05、06、11 及合并 DoD 分 host 可写持久化/memory 不可用提示；独立 Remote/本地偏好另验，fulfilled 不当保存成功。 |
| L1 原型计数 | 7/39、12/61 与内部七个设置分区不冲突 | 07 补统一口径，旧通过数不含新增未渲染状态。 |
| L2 107/115 链接 | 两次历史输入集合不同，不是算错；原文已描述历史时点，可进一步明确 | 08 注明合并日志及报告27集合，不覆盖旧数字，本轮另行统计。 |
| L3 roster 与路径 | ui-permission 对应 ui-permission-presets 属实；ui-cordis 在 extensions；dockkit 为 baseline | 03 W 表增加逐项 row id/装配来源，C0 明列包名/路径/静态 id/动态来源；D 五包 row id 也列出。 |
| L4 command-input 阶段 | 专属 Goal 行在 W7/C15，需澄清中间阶段 | G06/C7 明确未挂对应插件时 unknown 为阶段性预期，不算 D7 command 结果丢失、不冒充最终完成。 |
| L5 分派白名单 | 当前集中 RowKey/映射属实，但原文明确是拟议改造，不能据此撤销动态扩展目标 | G06/04 补一次性修改现有类型、分派、组件表和入口循环；后续贡献仍须合法类型/key，不能只加注册就宣称现状支持。 |
| L6 冻结引用 | 引用可更明确，R23/R28 是提交冻结而非所有命令流程的现成证明 | 05 直链第一优先约定和切换配置，新增命令规则仍归 G02，不夸称第一优先已覆盖命令。 |

## 关键证据

- [auto 双面 Loader 装配](../../../../packages/host/directory-picker-auto/src/index.ts)：SURFACE_PACKAGES 与 BACKEND_PACKAGES 一并 loader.create；已用 git show 核对 906b6de1e2 中也存在。无静态 row 不能推导无客户端 UI。
- [trigger 窄接口](../../../../packages/client/ui-input-trigger/src/client/contract.ts)、[来源及 guard](../../../../packages/client/ui-input-trigger/src/types.ts)、[检测](../../../../packages/client/ui-input-trigger/src/core/detect.ts)：现有 tier 不能只保留斜杠。
- [命令类型合并](../../../../packages/client/ui-commands/src/client/index.ts)与[实现](../../../../packages/client/ui-commands/src/client/service.ts)：popupFor 已类型化。全仓检索 bindComposerFocus 调用仅测试；这只是调用事实，不是禁止调用规则。
- [工作区入口](../../../../packages/client/ui-workspace/src/client/index.ts)、[uiWorkspace](../../../../packages/client/ui-workspace/src/client/navigation.ts)、[sessions 提供者](../../../../packages/api/session-controller/src/client/sessions/service.ts)：分别核对注入与提供。
- [settings 模式选择](../../../../packages/client/ui-settings/src/client/index.ts)、[scope 写入队列](../../../../packages/client/ui-settings/src/client/settings-scope.ts)：memory 不执行持久化队列；settingsSchema 独立服务，describe 为镜像访问方法。
- [Web 装配](../../../../packages/bundle/web-app/cordis.patch.yml)：ui-permission 的 name、ui-schedule 禁用状态与 directory-picker-auto；[只读调度](../../../../packages/client/ui-schedule/src/client/index.ts)、[调度工具](../../../../packages/schedule/schedule/src/tools.ts)和[调度类型](../../../../packages/schedule/schedule/src/types.ts)共同确定管理范围。

未把外部报告的“已独立核查”作为事实替代源码；未采纳其建议删除 picker 对应包、改 DoD 数或扣掉 W10 工期。独立复核还修正了 04 对交互槽当前归属与 G09 拟议迁移的表述冲突。

## 验证结果

本轮核查 14 份文档的 192 个本地引用路径全部有效；H/M/L 共 19 条意见完整且唯一，原 DoD 确认为 8+6，T01–T48 全部保留。记录在同目录 logs/29-review-static.log。

| 实际命令 | 结果 | 同目录 logs 下日志 |
|---|---|---|
| pnpm run test:docs | 16 通过，0 失败/跳过 | 29-review-test-docs.log |
| pnpm run doc-sync | 34 通过，0 失败/跳过 | 29-review-doc-sync.log |
| pnpm run lint | 退出码 0，含脚本构建前置及 contracts-ready | 29-review-lint.log |
| git diff --check | 退出码 0；既有 CRLF 提示不算失败 | 29-review-diff-check.log |

业务、浏览器、HMR、在线 API 和性能未运行；Lint 构建前置不替代产品验收。新增公开接入、冻结汇总和 QS 宿主仍是待实施方案，未因本次文档检查而解除运行验证。
