# 剩余依赖公告可达性核查

日期：2026-09-24。本次重新执行 `pnpm audit --json`，退出 1，结果为 0 critical、2 high、17 moderate、3 low。原始结果保存于 `logs/repair-v1-security-reachability-refresh.json`。安全扫描仍未通过；本报告不把尚未证明可达的公告判定为误报，也不把历史功能测试通过视为漏洞不存在的证据。

## 核查结论

| 依赖与安装版本 | 实际证据 | 当前判定及下一步 |
| --- | --- | --- |
| extract-zip 2.0.1 | 桌面运行时准备脚本直接使用；调用点防护及三个真实 ZIP 回归见 [172](172-桌面ZIP安全缓解验证.md)。本次 registry 查询最新版本仍为 2.0.1，查询 2.0.2 返回 ERR_PNPM_PACKAGE_NOT_FOUND。 | 两项高危仍在依赖扫描中；直接调用点有缓解，不能扩展为所有间接调用均安全。audit 的 patched_versions 字段不能证明修复包已发布，不能写入不可安装的版本。 |
| Mermaid 11.16.0 | 根清单、website 清单直接依赖；`scripts/verify-mermaid.ts` 对仓库 Markdown 调用 parse；`website/.vitepress/config.ts` 使用 withMermaid，配置 mermaid: {}。 | 文档校验及网站存在真实调用，不属于无用依赖。尚未证明恶意图能从工作台消息进入此实现，也未完成网站漏洞复现。11.16.1 已经 registry 查询确认存在，适合单独升级并验证全部图、文档构建和网站呈现。 |
| DOMPurify 3.4.11 | audit 路径为 Mermaid 的间接依赖；本仓应用源码搜索未发现 IN_PLACE 调用。 | 不能凭搜索无结果排除间接调用。需要检查 Mermaid 内部调用配置；升级 Mermaid 后仍须检查锁文件实际 DOMPurify 版本，不能假定间接依赖自动全部修复。 |
| Vitest / @vitest/mocker 4.1.8 | `vitest.web.config.ts` 为普通测试运行配置；本次已验收的 transcript-follow 使用 Playwright chromium.launch；应用和脚本源码未找到 mockerPlugin/interceptorPlugin 的直接使用。 | 没有建立公告描述的独立 mocker 插件暴露路径。普通 Playwright 浏览器测试不等于启用 Vitest Browser Mode。4.1.11 已查询确认存在；若升级须保持 coverage-v8 与相关 Vitest 包版本协调，并重新执行代表性测试。 |
| Hono / @hono/node-server / qs | audit 记录 MCP 等依赖链。`packages/mcp/mcp-client/src/transport.ts` 创建 StdioClientTransport 或 StreamableHTTPClientTransport；安装的 SDK HTTP 客户端使用 fetch 与 SSE。 | 不能把 MCP 客户端直接当成 Hono/Express HTTP 服务端。尚未完整遍历 SDK、模型提供者和构建工具的传递调用图，保留待核查；没有认定 HTTP 中间件公告已在工作台默认配置复现。 |
| protobufjs 7.6.4 | audit 记录 llm-pi-ai → pi-ai → google/genai → protobufjs。 | 安装在生产闭包是事实，外部输入能否进入 .proto option 解析尚无证据；需要继续定位 Google 提供者的实际解析入口，不能按普通模型响应推定可利用或不可利用。 |

搜索范围包含 packages、apps、scripts、website 的 TS/TSX/JS/MJS，排除 node_modules、lib、dist 和桌面生成目录；搜索只用于寻找直接调用，不是传递依赖可达性证明。另读取 MCP 包自己 node_modules 下的 SDK 源码，避免误把根目录没有提升安装的路径当成依赖不存在。

## 已核对的公告触发条件

Mermaid CSS 注入涉及图源可控及 SVG 周围的 DOM 结构；不能仅凭默认安全级别就关闭风险。已安装版本落在受影响区间，修复版为 11.16.1。[GHSA-6x64-9x62-f2gx](https://github.com/advisories/GHSA-6x64-9x62-f2gx)

DOMPurify 的该项公告要求 IN_PLACE 与移除容器的 hook 组合；普通 sanitize 调用和该条件不等价，修复版为 3.4.13。[GHSA-55q2-fjhq-7xh7](https://github.com/advisories/GHSA-55q2-fjhq-7xh7)

Vitest 公告区分独立 mocker/interceptor 插件的无认证 HMR 路径与自带 Browser Mode 的带令牌 RPC；不能把前者的无认证描述直接套用到所有测试运行。修复版为 4.1.11。[GHSA-82fw-gwwq-j7x9](https://github.com/advisories/GHSA-82fw-gwwq-j7x9)

## 验收状态

本次没有修改依赖清单、锁文件或业务源码，没有重新声称全量测试通过；此前组合修复的全量测试证据仍归 [182](182-同页面连接重装与作用域恢复.md)。新增内容仅为 QS 核查报告，不登记到官方原文件改动清单。

本次 `pnpm run test:docs` 退出 0，16 项通过、0 失败、0 跳过，15.85 秒；日志为 `logs/repair-v1-security-reachability-docs.log`。这是仓库文档快速门禁的结果，不替代公告利用条件的人工核查，也不表示对每条报告结论做了自动验证。

安全项尚未关闭。下一步按真实入口继续完成间接依赖核查，再分别实施可安装的补丁与对应回归。原生 Ctrl+F、原型全状态与其余性能场景也不因本报告关闭；W11 保持用户确认的本期延期，不接入真实登录。
