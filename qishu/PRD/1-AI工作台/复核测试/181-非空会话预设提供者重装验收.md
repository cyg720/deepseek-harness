# 非空会话预设提供者重装验收

日期：2026-09-24。补充 [174](174-预设提供者热卸载与持久恢复.md) 中非空会话的独立验证；不代表同页面连接服务替换或全部界面状态通过。

## 场景和结果

使用真实 Web Loader、Host 持久化与 Chromium。导入已结束一轮的记录，通过种子工具明确指定 `standard` 预设，同时将新会话全局默认设为 `ptc`。登录后打开历史会话，填写未发送草稿，热卸载并恢复真实 `agent-presets` 条目，通过设置刷新目录，全程不刷新页面。

断言历史会话只显示只读预设标签、没有可编辑预设选择器；恢复后名称和草稿保持一致，持久头仍为 `standard`，历史事件与生命周期操作前一致，发送请求为零、页面异常为零。首次打开种子会话的合法 `session/end-seed` 单独等待并严格断言，未过滤或吞掉其他新增事件。

`apps/web/tests/qs/preset-lifecycle.e2e.ts` 两项浏览器用例通过，退出 0，8.19 秒。日志：`logs/repair-v1-preset-existing-browser-final.log`。第一次失败因未使用种子函数的显式预设参数，第二次因比较起点早于合法结束种子事件；均修正测试夹具，未修改生产实现或放宽预设、消息和持久化断言。

## 验证范围

本批只新增 QS 测试，不修改官方原文件，不进入官方源码改动记录清单。最终 Windows `pnpm run typecheck`、定向 oxlint 均退出 0；日志为 `logs/repair-v1-preset-existing-types-final.log`、`logs/repair-v1-preset-existing-lint-final.log`。未调用真实模型、未接入真实登录、未提交或推送。连接实例替换、原生 Ctrl+F、原型全状态和其他剩余性能场景继续单独验收。
