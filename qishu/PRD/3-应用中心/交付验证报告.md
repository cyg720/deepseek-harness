# 应用中心需求交付验证报告

文档版本0.1.0，验证日期2026-09-14。本文只报告文档与本地合成数据验证，不表示业务验收通过。

交付正文：[应用中心.prd.md](应用中心.prd.md)、[离线HTML](应用中心.prd.html)、[完整SQL](应用中心.schema.sql)。原始[DDL.md](DDL.md)保留用户当前内容。

本轮用户确认单企业部署及审批权限由授权中心配置；自审、团队映射、凭据和首次登记active窗口、依赖变更保护、停止同步窗口、数据库、删除、运行参数和装载适配保留待决策。

## 完善内容

正文包含一图统揽、十二章需求、3张流程/状态图、7个用户故事、22条业务规则、19组接口定义、4张候选数据库表、28条详细TC、28项逐项AC和4组合成TD。原始业务版本与平台并发version分离为releaseVersion/version；补齐封面、可见归属、审批快照、依赖报告、凭据引用、持久同步和异常恢复。

单企业和审批权限来源已记录用户原话；审批是否允许自审仍待决策。新增接口和数据库均标候选设计；授权中心现有需求中的首次active登记和串行状态同步没有被静默改写。Q-05首次登记就激活、Q-06依赖检查后的变更、Q-07旧同步请求恢复是关键放行阻断。

## 实际验证结果

| 层级 | 结果 | 工具与证据 |
| --- | --- | --- |
| 内容静态检查 | 24项通过，0失败 | Python 3.11.15；[静态验证.json](validation/静态验证.json) |
| 双格式一致性 | 完整模型相同，第四章SQL与附件逐字一致；7份JSON示例可解析，表列注释覆盖检查通过 | [验证文档.py](validation/验证文档.py)；不等于数据库语法或约束已执行 |
| 测试数据复现 | 两个独立临时目录生成字节相同；不同内容覆盖被拒绝且原文件保留；数据数量符合正文 | 4组TD；[夹具](测试数据/v0.1.0/application-fixtures.json)；SHA-256为f9d7a9fcc05e3b28e6a551455cd9f4073f37c28c9610db5b3bfdf70d7f48a11c |
| 实际HTML离线浏览器 | 18项通过，0失败；file://真实打开，3张原位SVG；未记录隐式网络请求和未处理页面异常 | Chromium 149.0.7827.55，Playwright；[浏览器验证.json](validation/浏览器验证.json)、[脚本](validation/验证HTML.cjs) |
| HTML交互 | 多级目录覆盖/折叠/键盘定位、搜索清空、章节与全文编辑、Esc取消、批注/勾选写回、MD/HTML导出重开、ZIP/Ctrl+S下载、导入取消与非法/超限输入、缓存失败降级、恶意文本/非法图局部失败均通过 | 验证过程的修改仅在validation往返产物，正式PRD没有合成批注或测试勾选 |
| 视觉检查 | 桌面1440×1050、窄屏390×844与打印样式验证；窄屏无页面整体横向溢出，打印保留图形 | [桌面](validation/桌面.png)、[一图统揽](validation/一图统揽.png)、[窄屏](validation/窄屏.png)、[打印样式](validation/打印样式.png) |
| ZIP往返 | 两个实际下载ZIP均通过CRC及MD/HTML同快照检查 | [ZIP验证.json](validation/ZIP验证.json) |
| 业务TC与AC | 全部未执行；待决策项和隔离装载适配阻断业务放行 | 正文第九、十、十二章；没有业务服务访问或真人签署 |
| 数据库/迁移/负载 | 未执行 | 未连接数据库，未执行schema.sql或跨库写入，也未运行真实负载测试 |

浏览器用例的缓存失败由明确注入的SecurityError模拟；没有将模拟结果说成所有浏览器天然支持。跨标签缓存竞争、缓存基线恢复冲突、全部Mermaid指令攻击变体、所有浏览器/辅助技术未穷尽验证；本工具不提供真正多人实时协作。离线HTML会禁用正文中的相对文件链接，来源和附件请从Markdown或交付目录打开；完整业务正文、DDL与数据脚本已经内嵌，不依赖这些链接读取。

## 实际执行命令

工作目录为仓库根目录，浏览器脚本使用本机已安装的Playwright和Chromium，路径在脚本中可核对。以下命令只生成或验证文档/合成文件，不启动业务服务。

```powershell
python -X utf8 "applications/PRD/3-应用中心/validation/编制文档.py"
python -X utf8 "applications/PRD/3-应用中心/测试数据/生成测试数据.py" --out-dir "applications/PRD/3-应用中心/测试数据/v0.1.0"
python -X utf8 "applications/PRD/3-应用中心/validation/验证文档.py"
node "applications/PRD/3-应用中心/validation/验证HTML.cjs"
git diff --check -- "applications/PRD/3-应用中心"
```

编制文档.py只记录本轮初次编制快照；未来修订以应用中心.prd.md或HTML中当前Markdown为准，不重跑旧编制脚本覆盖新内容。SQL和数据脚本变更须同步第四、九章并重新构建双格式。

## 阅读与后续评审

双击应用中心.prd.html可离线阅读、搜索、编辑章节、增加批注并导出MD/HTML或双格式ZIP。浏览器修改不会静默写回原路径，关闭前应显式导出；本机草稿缓存默认关闭。勾选验收或填写批注不构成业务批准。

优先评审第十二章Q-02、Q-03、Q-05、Q-06、Q-07，再确定字段、数据库、删除、运行指标、媒体和测试装载。原始DDL.md及相邻平台、授权中心文档未被本轮覆盖。
