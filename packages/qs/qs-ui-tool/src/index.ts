/**
 * 工具调用呈现组件的 Host 入口（预留职责：工具调用树与逐工具视图）。
 *
 * 角色定位：转写内 tool-call 行的行组件与 qs.tool.call.toolview 子槽的声明者。
 * 当前状态：本包没有宿主侧行为，不注册服务、不读写会话状态。
 */

/** 注册本包的宿主侧行为；本包无宿主侧职责，保持空实现。 */
export function apply(): void {}
