import { clientBundle } from '../../client/tsdown.client.ts'

/** Build the independently mounted browser plugin. */
export default clientBundle('@deepseek-ai/dsh-qs-ui-brand', ['lib/types/index.js'])
