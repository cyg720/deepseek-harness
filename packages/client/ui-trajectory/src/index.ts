/*
 * ================================ 文件注释 ================================
 * 【文件职责】浏览器端轨迹插件的宿主加载入口。
 * 【技术维度】极简插件骨架：仅导出空 apply()。
 * 【产品维度】浏览器端插件的实际逻辑全部在 client 半区，宿主侧无行为。
 * 【逻辑维度】单个空 apply 函数。
 * 【关键边界】无宿主侧行为，纯占位。
 * 【新手阅读建议】与 client/index.ts 对比，理解"宿主半区 vs 浏览器半区"的划分。
 * ==========================================================================
 */
/** Host loader entry for the browser-only trajectory plugin. */

/** Provides no host-side behavior. */
export function apply(): void {}
