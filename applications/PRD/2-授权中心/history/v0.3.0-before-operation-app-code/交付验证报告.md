# 授权中心需求方案 0.3.0 交付验证报告

使用 `prd-writer-v3.1/prd-writer`（技能元信息 **3.1.1**）重新生成，工作区修订号 **3**。本轮原始 `PRD.md` 和 `DDL.md` 未修改，上一版正式交付文件保存在 [历史目录](history/v0.2.0-before-ddl-v3.1.1/授权中心.prd.md)。

## 交付内容

| 文件 | 内容 |
|---|---|
| [授权中心.prd.html](授权中心.prd.html) | 最新完整方案，六张 Mermaid 图离线渲染；二至六级多级目录可折叠并直达标题 |
| [授权中心.prd.md](授权中心.prd.md) | 同快照正文，保留141条独立测试用例、10组测试数据、30项验收点和全部原编号 |
| [授权中心.schema.sql](授权中心.schema.sql) | 14张业务表、1张日志默认分区、158条字段注释、26条显式索引；主键/UNIQUE隐式索引另外说明 |
| [授权中心.manifest.json](授权中心.manifest.json) | 原资料、技能及交付文件哈希和版本 |
| [生成测试数据.py](测试数据/生成测试数据.py) | 保留可复现生成方法，完整源码亦在正文9.5节 |
| [合成数据样例](测试数据/baseline/authorization-fixtures.json) | 保留33条表字段样例和其他测试上下文，认证字段为不可用标记 |

第四章4.5节包含与附带SQL逐字一致的完整脚本，4.6列索引/查询依据和维护代价，4.7将DDL检查关联到TC/TD/AC。该脚本是原模型的空库建表修订草案：修复10处缺逗号和重复索引，去掉7个已有UNIQUE覆盖的普通索引，补齐数据库注释并显式列外键删除/更新动作。

日志主键 `(id,created_at)` 为满足分区要求的候选方案，尚需Q-09/Q-16批准；单列id没有唯一约束。组织归属、服务凭据、令牌族、审批、并发版本等目标模型缺口仍保留Q，并未虚构为原有列。未提供真实旧库结构和数据快照，因此未生成冒充可执行的存量迁移SQL。

## 实际检查与证据

本次 149 项静态/数据/导出检查、30 项浏览器检查通过，0 项失败。[DDL目录定位截图](validation/v3.1.1/ddl-navigation.png) 已作目视核对。

- `python -X utf8 applications/PRD/授权中心/validation/v3.1.1/check_delivery.py`：见 [静态与数据检查](validation/v3.1.1/delivery-check.json)。检查双格式同快照、来源哈希、所有编号、用例/验收记录、完整SQL/注释/索引/FK，以及生成器可重复性、非法参数和覆盖保护。实际从正文提取生成器运行，并在临时目录生成10000条资源；不代表容量或性能已批准。
- `node applications/PRD/授权中心/validation/v3.1.1/check_browser.cjs`：见 [浏览器报告](validation/v3.1.1/browser-report.json)。断网、原生file://与隔离Chromium验证多级目录覆盖全部实际标题、父级收起/展开、DDL子标题准确定位、六张SVG、修改重绘、导入/编辑/批注、MD/HTML/ZIP导出重开、缓存、搜索和390px视口。
- `git diff --check`：本轮对需求Markdown及manifest的检查无空白错误。

本版证据在 `validation/v3.1.1/`；旧 `validation/` 与 `validation/v3.1/` 报告属于旧版，不用于证明本次交付。所有浏览器合成编辑和批注只写验证副本，正式需求保持待决策状态。

## 使用与验证范围

打开主HTML后，父目录文字点击会定位并切换子目录，箭头只控制折叠；小目录点击直达对应标题。目录默认展开，正文重建后重新展开。第四章4.5可阅读/复制完整SQL，独立SQL文件在同目录。

数据库操作、初始化、迁移、约束拒绝、实际查询计划、授权中心业务测试及验收均未执行；30项验收记录继续为阻断/未执行。SQL未包含原始默认账号初始化数据。测试材料不是可直接登录的凭据，也不是生产装载种子。

浏览器验证仅代表报告所列Chromium、桌面和390px模拟视口；未验证其他浏览器、真实手机、辅助阅读器、实际打印机或浏览器退出后的缓存持久性。旧独立图示HTML保留为历史副本，本版图形以主HTML为准。
