# 授权中心需求文档交付与验证报告

已依据 [原始 PRD](PRD.md)、[原始 DDL](DDL.md) 和 [prd-writer v3.0](../skills/prd-writer-v3.0/prd-writer/SKILL.md) 生成同快照 Markdown 与单文件离线 HTML。文档状态为“待决策”，包含原文 14 项功能、30 条业务规则、30 条验收标准、17 组接口操作定义、30 组测试计划、18 项 DDL 问题及 16 项待决策事项。接口方法、URL 和业务码未得到资料支持，已明确留待核验，没有冒充现有接口。

## 交付文件与使用

| 文件 | 用途 |
|---|---|
| [授权中心.prd.md](授权中心.prd.md) | 完整需求正文，12 章及修订、评审记录；可用 Markdown 编辑器修改 |
| [授权中心.prd.html](授权中心.prd.html) | 同一正文的离线工作台；在允许本地 HTML 的浏览器中双击打开 |
| [授权中心.manifest.json](授权中心.manifest.json) | 文档 ID、业务版本、生成时间、文件大小与 SHA256；不是批准签名 |
| [validation/structure-check.json](validation/structure-check.json) | 原文件完整性、章节/编号/JSON/表格/同快照检查结果 |
| [validation/build-tests.txt](validation/build-tests.txt) | 技能构建器 13 项测试的 UTF-8 模式执行结果 |
| [validation/browser-report.json](validation/browser-report.json) | 27 项实际浏览器验证及环境、限制 |
| [validation/export-check.json](validation/export-check.json) | ZIP CRC、Markdown/HTML 完整快照及测试后文件完整性 |

HTML 可全文查找、点击目录、编辑章节或完整 Markdown、添加评审批注、导入 MD/JSON、导出 MD/HTML/双格式 ZIP。修改后建议点击“导出双格式包”，再确认浏览器下载完成；网页不会静默覆盖原始文件。文件中的待决策状态、验收勾选和批注不等同于批准或测试执行。本机草稿默认关闭，导出才是明确的文件保存方式。

原模板将相对文件链接显示为禁用链接；HTML 正文完整可读，原始资料、技能或本报告请从文件夹或 Markdown 版本打开。HTML 中的外部资料链接由用户主动确认后访问，离线核心功能不依赖这些链接。

## 需要优先评审的内容

| 事项 | 已发现的问题 | 需求正文定位 |
|---|---|---|
| DDL 建库障碍 | 10 处列间缺逗号、重复创建 idx_token_account_id、按 created_at 分区但主键仅 id | 第四章 DB-01—DB-03；静态审查发现，未执行 SQL |
| 组织权限 | 缺少账号组织归属；DEPT 的“所属部门”与“本部门及以下”两种表述冲突 | Q-02、DB-04、DB-07 |
| 应用权限组合 | 应用一组/多组、应用操作上限与用户权限的交集、共享组变更影响未明确 | Q-03 |
| 数据与委托分支 | 原文只有静态权限失败才查委托；静态有权但目标行超范围、列表无资源 ID 等需确认 | Q-04 |
| 身份与令牌 | 服务账号凭据/证书绑定缺失；1 小时适用对象、刷新配对、退出/撤销和冻结传播未明确 | Q-05、Q-06、Q-08 |
| 委托与审计 | 委托审批/拒绝/撤销、应用隔离、可授出权限未闭环；未知身份拒绝日志和归档恢复未定义 | Q-07、Q-09 |

建议先评审上述业务和安全问题，再定稿接口与修订 DDL。其他并发、删除、时间、配置、性能和迁移问题均在第十二章记录责任角色与阻断范围；具体人员、期限和批准证据尚未提供。

## 实际执行结果

执行日期：2026-09-08。环境：Windows；Python 3.11.15；Node v24.18.0；浏览器 Chromium 149.0.7827.55，使用隔离自动化上下文，断网并通过原生 file:// 路径访问交付文件。执行主体为 Codex 自动化和静态复核，不是业务验收人员。

| 验证层级 | 结果 | 证据与解释 |
|---|---|---|
| 文档静态检查 | 64 项通过，0 失败 | 完整十二章、US/BR/AC/API/TC/Q/DB 编号及引用、表格列数、3 个 JSON 示例、代码围栏、UTF-8/LF/末尾换行、本地资料链接、14 表覆盖和原文件哈希 |
| 原技能构建器测试 | 13 项通过 | 使用 Python UTF-8 模式；包括双格式、元信息、CSP、输出覆盖保护、恶意内容转义等；这些是构建器测试，不是业务测试 |
| 同快照生成 | 通过 | Markdown 和 HTML 解析后得到相同 docId/version/revision/updatedAt/markdown；HTML 含完整正文 |
| 离线浏览器 | 27 项通过，0 失败 | 原生本地打开、完整正文、搜索/空态/目录、编辑/取消/焦点、批注、勾选、MD/HTML/ZIP/快捷键导出、重新打开及导入保护 |
| 安全与缓存验证 | 已包含于上述 27 项 | 脚本/事件属性/脚本链接/追踪图片/恶意元信息未执行；无隐式网络请求；缓存不可用时仍可导出；真实隔离 localStorage 写入、重开恢复及双标签竞争验证通过 |
| 响应式与打印样式 | 通过已测范围 | 1440×1000 桌面、390×844 模拟窄屏；窄屏无页面横向溢出；Esc 收起目录；打印 CSS 隐藏控件并显示全文 |
| 控制台与页面异常 | 0 条 | browser-report.json 中 pageErrors、consoleErrors 和 implicitNetworkRequests 均为空 |
| 双格式 ZIP 独立核验 | 2 个测试包通过 | 浏览器下载的 roundtrip.zip、keyboard.zip CRC 正确，均含两份文件，正文与修订号一致 |
| 原始资料与正式产物保护 | 通过 | 原 PRD/DDL 哈希未变化；浏览器测试未修改正式文件，合成批注和任务勾选仅在 validation 副本 |
| Git 空白检查 | git diff --check 通过 | 该命令不覆盖未跟踪的 applications 文件；新需求文件的空白/换行由定向检查另行验证 |
| 业务/API/数据库/迁移/性能测试 | 未执行 | 没有授权中心服务、实现版本、隔离数据库或批准的目标；TC-01—TC-30 均是计划 |
| 正式业务批准/验收 | 未执行 | 没有真人签署或验收证据；文档保持待决策 |

本轮执行过程中，构建器测试默认 Windows GBK 编码使 3 个含 emoji 的临时测试样例无法写入；使用 `python -X utf8` 后 13 项全部通过，未修改技能文件。定向验证脚本还修正了 CSP 的 HTML 实体读取方式，以及缓存故障后复位勾选框的测试操作；最终结果如上。这些调整没有改变需求正文或离线工作台实现。

## 可复核证据

| 证据 | 内容 |
|---|---|
| [桌面截图](validation/desktop.png) | 正式需求文档的初始阅读状态 |
| [窄屏截图](validation/mobile.png) | 390px 视口下的文档阅读布局 |
| [打印样式截图](validation/print-style.png) | 浏览器打印媒体样式；不是 PDF 交付 |
| [生成与结构核验脚本](validation/build_and_check.py) | 复用原技能 render_prd.py，检查和生成当前正式双格式文件 |
| [浏览器核验脚本](validation/check_browser.cjs) | 在离线 file:// 页面验证本次交付；含本机已核验运行时路径 |
| [导出独立核验脚本](validation/verify_exports.py) | 用 Python 独立检查 ZIP、同快照及原始/正式文件哈希 |

本次成功运行的定向命令如下，工作目录为 deepseek-harness 仓库根目录；浏览器脚本使用其源码中记录的本机 Playwright/Chromium 路径，其他机器需先核验并调整路径。

```powershell
python -X utf8 'applications/PRD/授权中心/validation/build_and_check.py'
python -X utf8 'applications/PRD/skills/prd-writer-v3.0/prd-writer/scripts/test_build.py'
node 'applications/PRD/授权中心/validation/check_browser.cjs'
python -X utf8 'applications/PRD/授权中心/validation/verify_exports.py'
git diff --check
```

没有执行仓库代码构建、全量单元测试或 doc-sync；本次产物是 applications 下的独立业务需求文件，未修改 Harness 代码或其发布文档。也没有执行 SQL、登录业务系统、部署、上传、发消息或提交 Git。

## 原始资料完整性

2026-09-11 提交检查清理了原始 PRD/DDL 的尾随空白和末尾空行，正文内容保持一致。`validation/build_and_check.py`、manifest 和静态检查报告使用清理后的 SHA256，64 项静态检查及导出包独立核验已重新通过；下表保留 2026-09-08 交付验证时的原始摘要。

| 文件 | SHA256（读取前后相同） |
|---|---|
| DDL.md | 55af7255e5f3f4536059607a29c3ad1f9bd0f5c05a7e3b2246a4f7e5bdd38fcd |
| PRD.md | f4a18bafb5d2651e0e13377f9dc80955ea29878f6a4ad4d5aa7373d19946300a |

## 未覆盖边界

尚未验证 Firefox/Safari、真实手机、辅助阅读器、实际打印机、浏览器退出后的缓存持久性、系统存储配额及企业浏览器策略。缓存故障采用显式故障注入，基线不一致采用合成缓存记录；真实恢复/双标签测试仅覆盖本次隔离 Chromium 环境。不声称支持可信多人协作、云端保存、登录鉴权或电子签名。

validation 内的 roundtrip、metadata-security、cache-* 等文件包含自动化测试内容，仅用于复核，不是正式需求版本。日常阅读与评审使用目录根部的授权中心.prd.md 和授权中心.prd.html。

需求来源覆盖、编号和格式检查不能证明所有业务规则都正确；第四章 DDL 问题也未通过数据库执行复现。正式实现和发布仍需责任角色关闭对应 Q，完成业务测试与验收记录。
