# PRD Writer v3.1 验证报告

执行日期：2026-09-12T03:57:39.300265+00:00。技能元信息版本：3.1.0；目录保留 prd-writer-v3.0。

## 结论与范围

构建器 16 项、测试数据生成与引用 4 项全部通过。浏览器在原生 file:// 模式下 24 项通过，content 注入模式下 25 项通过；两组使用相同回归用例，content 组额外验证自然缓存不可用。没有执行业务代码、业务 SQL、业务写接口或正式验收。

本地文件导航、原位 Mermaid SVG、编辑/导入/导出重开、错误隔离、连续修订竞争、移动布局和打印样式均实际验证。自动化原生文件导航不等于操作桌面双击，也不代表所有浏览器兼容认证。

## 执行环境与命令

- Windows，Python 3.11.15，Playwright Python 1.58.0，Chromium 149.0.7827.55（无头）。
- 视口：1440 × 1100、390 × 844；浏览器上下文离线。
- 生成器仅使用 Python 标准库；浏览器测试依赖装入临时测试目录，不是 HTML 的外部运行依赖。
- 两组浏览器测试均观察到 0 次 HTTP/HTTPS/WS/WSS 请求、0 个未处理页面异常、0 个控制台错误。

在 prd-writer 目录执行（浏览器路径换为实际安装位置）：

```bash
python scripts/test_build.py
python scripts/test_test_data.py
python scripts/test_runtime.py --chromium C:/Users/18010/AppData/Local/ms-playwright/chromium-1228/chrome-win64/chrome.exe --mode file --out-dir ../validation
python scripts/test_runtime.py --chromium C:/Users/18010/AppData/Local/ms-playwright/chromium-1228/chrome-win64/chrome.exe --mode content --out-dir ../validation/content
```

技能创建器的 quick_validate.py 检查通过；Python/工作台 JavaScript 语法、31 个评测定义编号唯一性和技能本地引用检查通过。README 中英文及实现记录的双语配对检查通过。只对当前修改执行相关检查，不将其描述为全仓库测试通过。

## 构建与测试数据

[构建原始结果](validation/build-tests.txt) 覆盖正文往返、结束标签注入、两个内嵌脚本的 CSP 哈希、缺失/篡改图形库、模板脚本缺失/重复、许可证内嵌、输入校验及文件覆盖保护。

[数据生成原始结果](validation/test-data-tests.txt) 实际运行正文中的完整 Python 代码，在两个临时目录生成并比较哈希，核对合法基线、重复/空白/边界/null/缺失/旧版本/并发数据、状态转移、重跑及不同参数覆盖保护；同时检查 20 条 TC、5 个 TD 与 20 项 AC 的引用完整性。这些是文档与夹具检查，20 条业务 TC 本身均未执行。

已交付可运行 [数据脚本](prd-writer/examples/generate_device_test_data.py)、生成后的 [合成夹具](prd-writer/examples/test-data/run-a/device-fixtures.json) 和 [数据清单](prd-writer/examples/test-data/run-a/manifest.json)。夹具 SHA-256：d8948265ab0b26180dfc83093171452af678ef8d35668a76eafdc5c59c8a0426。

## 原生 file:// 浏览器结果

| 检查 | 结果 | 说明 |
|---|---|---|
| 离线内容渲染：15 个章节、表格、代码与两张 Mermaid SVG | 通过 | 按回归脚本断言执行 |
| 20 条用例与 20 项验收均以表格/详情呈现，完整数据脚本可见 | 通过 | 按回归脚本断言执行 |
| 全文搜索与清空恢复 | 通过 | 按回归脚本断言执行 |
| 章节编辑回写唯一正文 | 通过 | 按回归脚本断言执行 |
| Esc 取消章节编辑且不污染正文 | 通过 | 按回归脚本断言执行 |
| 完整源码编辑与实时预览 | 通过 | 按回归脚本断言执行 |
| 任务勾选写回 MD，不冒充验收 | 通过 | 按回归脚本断言执行 |
| 章节批注入正文并安全显示 | 通过 | 按回归脚本断言执行 |
| MD 实际下载与逐字往返一致 | 通过 | 按回归脚本断言执行 |
| HTML 实际下载、重新打开后正文与图形保留 | 通过 | file 模式将导出字节写为本地 HTML 并原生导航；content 模式仅注入。 |
| 双格式 ZIP 可解压、CRC 正确、同一快照 | 通过 | 按回归脚本断言执行 |
| Ctrl+S 导出双格式包 | 通过 | 按回归脚本断言执行 |
| 导出 MD 再导入，不丢失批注和正文 | 通过 | 按回归脚本断言执行 |
| 非法快照导入保留原文 | 通过 | 按回归脚本断言执行 |
| 取消导入不覆盖当前内容 | 通过 | 按回归脚本断言执行 |
| 草稿恢复与跨标签冲突保护（存储模拟） | 通过 | 使用标记的 Map 存储桩及合成 storage 事件，不声称真实 file:// 持久化验证。 |
| 恶意 HTML、数据结束标签、脚本链接与追踪图片 | 通过 | 按回归脚本断言执行 |
| Mermaid 全文/章节编辑、导出重开与导入重绘 | 通过 | 按回归脚本断言执行 |
| Mermaid 慢渲染与连续编辑不回写旧图 | 通过 | 人为延迟图形渲染，验证异步修订竞争。 |
| Mermaid 错误、配置/链接、超限与 HTML 输入局部处理并可修复 | 通过 | 按回归脚本断言执行 |
| 390px 移动布局无页面横向溢出、目录开关 | 通过 | 按回归脚本断言执行 |
| 打印样式隐藏控件并恢复全文 | 通过 | 仅浏览器 print 媒体样式，不是实际打印机/PDF 验收。 |
| 无隐式网络请求 | 通过 | 按回归脚本断言执行 |
| 无未处理脚本或控制台错误 | 通过 | 按回归脚本断言执行 |

原始结果：[file 模式](validation/browser-report.json)、[content 模式](validation/content/browser-report.json)。图形截图：[一图统揽](validation/mermaid-overview.png)、[状态图](validation/mermaid-state.png)；[桌面](validation/desktop.png)、[窄屏](validation/mobile.png)。

## 授权中心图形兼容检查

使用授权中心现有 Markdown，通过新版模板在临时目录生成 HTML 并以原生 file:// 离线打开：6 个 Mermaid 图位全部显示 SVG，0 个图形失败、0 个页面异常、0 次外部请求。仅验证图形兼容性，未改写授权中心原需求文件，也未验证该业务方案。

证据：[机器结果及源文件哈希](validation/authorization-mermaid-report.json)、[一图统揽截图](validation/authorization-overview.png)。

## 未覆盖与保留限制

- 31 项技能行为场景是评测定义，未做独立宿主/模型 A/B 评测；文档引用检查不能自动证明全部业务语义正确。
- 业务接口、数据库装载/清理、代表性性能规模/阈值及审计机制均未核验；演示中的相关用例与验收保持阻断。
- 成功缓存恢复及跨标签冲突使用明确标记的 Map 存储桩；content 模式的缓存不可用使用真实 SecurityError。未验证真实 file URL 缓存持久化。
- 未测试 Safari、Firefox、真实移动设备、辅助阅读器、实际打印机或企业下载策略；打印只验证 print 媒体样式。
- Mermaid 固定为 11.16.0，图形资源约增加 3.4 MiB；禁用文档配置、点击/链接指令、图片资源和外部样式，单图上限为 50,000 字符、500 条边。未验证所有图形类型和极端大文档。

[结构检查记录](validation/structure-check.json) 与 [使用说明](README.zh.md) 可用于后续复核。
